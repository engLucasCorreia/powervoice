# MEMORY.md — Project memory (curated by the orchestrator only)

## Status
- **PACING (2026-09-13):** owner reported 90 % of the session usage budget spent → only Slice 1 agents (S1-03, S1-04) keep running; **S3-01, S3-03, S4-02 paused** (TaskStop) with partial work in `.claude/worktrees/<id>` (uncommitted) — resume after the limit resets by dispatching a new agent told to continue from that worktree's state. Max 2 concurrent agents until the reset; keep orchestrator messages short.
- **VERTICAL SLICES (D-022, 2026-09-13, owner):** "Make something work first and after improve. Instead of getting all perfect from the beginning." → Work is organised as thin end-to-end **slices** (see `tickets/BOARD.md` top): S1 record & play → S2 edit → S3 clean/effects → S4 deliver → H hardening (full spec ACs, recovery, punch-in, spectral, perf, packaging) → P plugins (M8/M9). Slice tickets are lean and full-stack (Rust + UI in one ticket), implement the essential subset of the approved specs and list deferred ACs explicitly. No new spec waves until a slice needs one (short specs then). Opus review only for RT-audio and data-loss code, **blocking findings only, no re-review loops** (orchestrator verifies fixes); nits → backlog. Merge green work fast. The orchestrator smoke-runs the app after each slice.
- **AUTONOMOUS MODE (D-021, 2026-09-12, owner going to sleep):** "keep the development without asking me anything anymore … not doing checkpoints anymore … until the end of the tickets." → Run every milestone back-to-back with no owner checkpoints and no questions. Owner-facing choices use the documented recommended default and are logged as "D-0xx (autonomous)". Still gate every ticket (`just check`, Opus review where required). **T-811 VST2 stays gated** (needs explicit legal sign-off; skip it and report at the end). M8 deferred choices use defaults: sandboxed-plugin monitoring latency = accept + show (SPEC-002 OD-1 A); unsigned native modules = allowed with a warning dialog.
- **Current milestone:** M0 done (checkpoint passed 2026-09-12, decisions D-011…D-020). M1 starting after T-009 (code rename) merges; M2 spec wave (T-200) running in parallel.
- **Last checkpoint:** none (M0 is the first)
- **Status line superseded:** the "Next action" line below is historical; after owner answers, apply decisions to specs (OD boxes), mark specs/ADRs approved, then write full M1 ticket files (T-101…T-110) and dispatch M1 W1.
- **Next action:** M0 W1 running: T-001 (Haiku), T-002/T-003 (Opus, docs-only) — all in main tree, disjoint files. Then W2: T-006 after T-001; T-004 after T-001+T-002; T-005 after T-001+T-003.

## Environment
- Rust 1.98.1 stable via pacman `rustup` (+ rustfmt, clippy, x86_64-pc-windows-gnu target); `just` 1.58 via pacman; Node 26.7 / npm 11.19; git 2.55.
- Worktrees: the Agent tool's `isolation: "worktree"` fails in this session (session started before `git init`). Use manual worktrees: `git worktree add .claude/worktrees/T-NNN -b ticket/T-NNN main`; the agent works there and commits on its branch; the orchestrator squash-merges and removes the worktree.
- Orchestrator: never `cd` into a worktree in Bash — the harness then switches the session's primary working directory into it. Use `git -C <path>` and absolute paths / `--manifest-path`. If it happens anyway, `cd` back to the main repo **before** `git worktree remove` of that worktree (removing the shell's cwd breaks the shell).
- Squash-merging a ticket branch that predates other merges usually conflicts only in `Cargo.lock`: take `--ours` (main) and let `just check` re-resolve, then `git add Cargo.lock`.
- Edition 2024 reserves `gen` — don't use it as an identifier (testkit uses `signal`).
- Package naming: `vox-<crate>` for libraries (dirs stay `crates/<crate>`), `powervoice-cli` (bin `powervoice-cli`), `powervoice-app` (src-tauri).

## Decisions log
| ID | Date | Decision | Where |
|---|---|---|---|
| D-001 | 2026-09-12 | Tauri 2 + Rust engine + Svelte 5 UI; single-file mono editor; non-destructive rack; destructive normalize favorites | PROMPT §2 |
| D-002 | 2026-09-12 | External plugins CLAP/VST3/LV2/VST2/JSFX in M8, out-of-process sandbox; generic param UI first, native editors M9 | PROMPT §2, §3.7 |
| D-003 | 2026-09-12 | Specs written just-in-time per milestone (W0), approved at previous checkpoint | PROMPT §5 |
| D-004 | 2026-09-12 | New crates `rack` (pure chain host shared by engine/export/bake/CLI), `testkit`, `cli`; rack core + document model moved to M1 | PROMPT §4, §7 |
| D-005 | 2026-09-12 | Document model = disk-backed chunk store + Arc snapshots (piece table) + edit journal; undo = snapshot stack | ADR-004 (pending) |
| D-006 | 2026-09-12 | Installable modules packaged as CLAP bundles (`clack-plugin`) | ADR-006 (pending) |
| D-007 | 2026-09-12 | LAME (LGPL-2.0+) **loaded at runtime via `libloading`** with our own FFI table (mp3lame-encoder rejected: static-only); MP3 export disabled if lib missing. ysfx (Apache-2.0 lib) only in sandbox (crash isolation). VST2 gated, via Carla | ADR-007 |
| D-008 | 2026-09-12 | Module API per ADR-005 (`activate`/`process`/`reset`/`deactivate`, event lists with offsets, host-owned bypass, typed extensions); supersedes PROMPT §3.7 wording `prepare`/`tail_samples()`; `module-api` deps: serde + thiserror | ADR-005 |
| D-009 | 2026-09-12 | Installable modules = CLAP plugins via `clack-plugin` 0.2 in a `module-clap` crate, `org.powervoice.module-info/1` extension, `.voxmod` zip packages, run in the sandbox | ADR-006 |
| D-010 | 2026-09-12 | Sandbox: one process per plugin instance; pipelined realtime path (+1 block latency, reported), blocking offline path; failure → bypass + notice (playback), abort (export/bake) | ADR-008 |

## Conventions
- Ticket commits: `T-NNN: <summary>` squash-merged by the orchestrator.
- Model tiers: Haiku 4.5 (bootstrap/templates/docs), Sonnet 5 (UI/IPC/IO/editing/harness), Opus 5 (architecture/RT/DSP/plugins/reviews).
- Max 4 parallel agents per wave.

## Rules (enforced in review)
- RT allocation checks go **only** through `vox_module_api::test_util::no_alloc` (raw `assert_no_alloc::assert_no_alloc` is clippy-disallowed): `assert_no_alloc` runs in warn mode workspace-wide via feature unification, so raw calls would only warn and pass.
- Types promising "no reallocation" (fixed-capacity lists/buffers) must implement `Clone` by hand preserving capacity — derived `Clone` on a `Vec` drops spare capacity.
- T-103 handoff: (1) rack currently drops module output events — needs an RT drain to the control thread; (2) chain edits may move unchanged module instances from the retiring chain (pointer moves) instead of cold-restarting all slots; crossfade needs one extra `max_block` buffer; (3) module registry, same-offset/id event coalescing, dual-mono shim.

- Subagents must run long checks in the foreground and never end a turn waiting on a background monitor (happened in T-007 and S3-02; the orchestrator had to nudge them). Now a CLAUDE.md hard rule.

## Gotchas / learnings
- Svelte 5: an element bound (`bind:this`) inside `{#if}` is `undefined` in `onMount` if the branch is closed at mount — attach observers/listeners in an `$effect` keyed on the element, not `onMount` (S1-03 blank-waveform bug, fixed d2f81b8). jsdom has `clientWidth` 0 and no `ResizeObserver`: stub `clientWidth` in tests.
- Worktree behind main: after `git merge main`, run `npm ci --prefix <worktree>/ui` if main added UI deps (stale `node_modules` breaks svelte-check).
- S3-03: clippy `approx_constant` flags spec literals like Q 0.7071 — keep the spec value in a const with a scoped `#[allow]` (don't swap to `FRAC_1_SQRT_2`: changes defaults/state). Butterworth slope ACs are tested on the f64 dsp sections (f32 `process()` can't resolve −190 dB).
- Tauri 2: `core:default` does NOT grant `core:window:allow-destroy` — any `getCurrentWindow().destroy()/close()` needs the explicit permission in `src-tauri/capabilities/default.json`, or the call is refused and (with a close-requested guard) the window can never close. New window/webview calls: check the capability first.
- S4-02: `flacenc` 0.5.1 output decodes bit-exact but `flac -t` warns "frame number does not increase… might not be seekable" on multi-frame files (upstream quirk). `flacenc::config::Encoder` is `#[non_exhaustive]` (mutate `Encoder::default()`); `BitRepr::to_bytes()` is test-only — use `bitsink::ByteSink`. FLAC verify-before-rename (SPEC-005 §2.11) deferred until symphonia (T-202). Dev machine has lame/flac/ffmpeg installed, so MP3/FLAC tests really run here.
- S2-01: edits go `DocumentService::edit_*` → `Session::commit_edit` → `EngineHandle::set_document` (its internal engine stop keeps the heard position — don't send a user `TransportCommand::Stop`, that jumps to play-start). Check `is_recording()` BEFORE range validation so "refused while recording" wins. During a take the document is always empty at the `DocumentService` layer. Clippy `len_without_is_empty`: name length getters `len_samples()`. Selection drag-handle resize is deferred (hardening).
- S4-04: `job_progress` event + `JobKind`/`JobState`/`JobProgressDto` (src-tauri `ipc/events.rs`) is THE job channel — new long jobs (normalize, LUFS normalize, save-as-job) add a `JobKind` variant, don't invent events. A cancellable, progress-reporting offline render loop lives in `src-tauri/src/export.rs::render_with_progress` (built on public `vox_rack::offline::build_chain`/`Chain::process`); export currently renders `RackModel::default()` until the rack is wired (H-08).
- S2-02: until S2-02 NO frontend code listened to the backend `notice` event (device lost/reconnected, recording clip notices went nowhere) — `initNotices()` in `ui/src/lib/state/notices.svelte.ts`, wired in App.svelte, now does. New backend notices need only an i18n key. Normalize is synchronous (`DocumentService::edit_normalize_peak`, one undo entry `history.normalize`, `Edit::replace_with(.., MarkerMapping::Identity)` preserving Silence pieces); progress/cancel → H-09.
- S3-01: rack panel lives in `ui/src/lib/rack/*` (RackPanel/RackSlot/ParamGroupSection/ParamControl/AddModuleMenu + rack.svelte.ts), engine side `crates/engine/src/rack_api.rs`, IPC `ipc/rack_commands.rs`/`rack_dto.rs`. `EngineHandle::rack_model()` returns the LIVE RackModel (for H-08 export). Module/param/group i18n keys are NOT generated — a new module needs its `module.*`/`param.*`/`group.*` keys in en.json (dump them with a throwaway test over `builtin_factories()`). Filter `ParamFlags::BYPASS` and `HIDDEN` out of generic bodies. Add-module menu groups by a fixed CATEGORY_ORDER.
- S2-03: a marker command = build an `Edit` with `.marker(MarkerOp::{Add,Remove,Rename,Move})` + `Session::commit_edit` (ids via `Session::new_marker_id`). WAV markers: `vox_io::{read_wav_markers, write_wav_with_markers}` (hand-rolled `cue `/`LIST adtl` appended after hound's finalize, RIFF size patched). Marker-only edits bump `rev`, not `audio_rev`; the UI refetches `markers_get` on every `document_changed`. Keymap bindings have an `alt` flag (`bindingIdentity` includes it). Deferred: i18n'd default names ("Marker NN" is English in Rust), region→selection on jump, rename/Delete-key bindings, panel virtualization (10k markers).
- S3-06: noise-print capture = `NrCaptureService` (src-tauri `nr_capture.rs`, job-shaped like export: `JobKind::NrCapture`) → `EngineHandle::nr_capture_prepare/apply` → `RackHost::replace_noise_print` (crossfade path). Capture the `NoiseProfile` extension handle at `Loaded` construction — the instance is gone once handed to the audio thread. Fake-backend engine tests doing real DSP on a live rack MUST hold `fake.spawn_driver(..)` for the whole test, or the stall detector closes the output mid-test. `vox-engine` must not depend on `vox-modules` at compile time: duplicate a built-in module id as a const + dev-dep equality test. Slot targeting is index-based (rare prepare/apply race fails safely). Deferred [H]: Ctrl+Shift+P, profile graph, Clear Noise Print, export confirmation.
- S4-01: `vox_dsp::loudness::LoudnessMeter` is THE BS.1770 meter (mono, `ebur128` `precision-true-peak`); `crates/project` now depends on `vox-dsp`. Loudness analysis = `LoudnessService` (src-tauri `loudness.rs`, `JobKind::LoudnessAnalyze` + payload event `loudness_report`); "processed" mode renders through the live rack (`rack_model()`), "source" skips it. LUFS normalize (`DocumentService::edit_normalize_lufs`, undo `history.normalize_lufs`) measures the SOURCE and reuses peak-normalize's `write_gained`; the TP-ceiling check is exact arithmetic (measured TP + gain). `document_commands::run_blocking`/`after_edit` are `pub(crate)` for sibling command modules.
- H-08: export snapshots the live rack at `start_job` (`EngineHandle::rack_model()` → `Option<RackModel>`, `unwrap_or_default()`; note `ManualEngine::rack_model()` returns a bare `RackModel`). Export range Whole/Selection and MP3 VBR are in the dialog. SPEC-014 "Output noise only" confirmation is client-side from `rackState()` (param key `noise_only`, not bypassed), shown before the save dialog. Live-engine + open-document job tests: see `export.rs::live_engine_with_gain_minus_6_db` / `nr_capture.rs::live_engine_with_nr_slot` (FakeBackend::plug + spawn_driver + retry on rack Add).
- S4-03: ACX rules live in `vox_dsp::acx::evaluate` (RMS [−23,−18] dB, sample peak ≤ −3 dBFS, noise floor = quietest 500 ms ≤ −60 dB; inclusive with 1e-6 dB epsilon — testkit generators are only ~1e-9 exact). `acx_check` is a synchronous command (whole document, processed or source). The block-render primitive for offline analysis is `render_processed` in src-tauri `loudness.rs` — reuse it, don't re-derive the loop.
- H-05: capture never splices — ring overflow ends the take at the last sample that fit (`StopReason::Overflow`, notice `notice.record.overflow`); a writer error sets `InputShared::writer_failed` and the control thread stops with `StopReason::WriteError`; `stop_reason` is set by compare-exchange from USER (first reason wins). `recover_take` tolerates a torn newest part (≤ 44 bytes). Crash tests: the test binary re-runs itself (`--exact <child_test> --nocapture`, env-gated) and the parent `Child::kill()`s it; declare the temp dir before the child guard. `ManualEngine`: `fake.advance_by(big)` without `tick()` stalls the inline writer (useful to force overflow). Follow-ups → H-10.
- S3-07: typed module extensions (NoiseProfile, ResponseCurve) are captured at `Loaded` construction in `RackHost` and exposed per slot (`SlotInfo::curve_handles`, `noise_profile`) — reuse that recipe for a third extension. `RackCommand::SetParamPlain` + `EngineHandle::response_curve(index, freqs)` (≤ 512 points, JSON `rack_response_curve`; VXRC binary is hardening). EQ graph math lives in canvas-free `ui/src/lib/eq/*.ts` (jsdom has no 2D canvas; jsdom rAF is a real ~16 ms timer — await a setTimeout in tests). "Import lint" tests via `import.meta.glob(..., { query: "?raw" })`. TS `noUncheckedIndexedAccess` is on. Deferred: Alt+click / reset-on-double-click split, context menu, keyboard nav, expanded view, analyzer overlay.
- H-02: dither lives in `vox_dsp::dither` (`StreamingQuantizer`: every `push()` but the last must be a multiple of `DITHER_BLOCK_SAMPLES` = 4096 so dither blocks line up with one-shot; `CHUNK_SAMPLES` 65 536 satisfies it). `vox_io::WavStreamWriter` (open → `write_block` → `finish`/`abort`); `save_snapshot_wav` streams 65 536-sample blocks from `SnapshotReader`. Save-output golden hashes: `crates/io/tests/golden_save.rs` (hash file bytes, xorshift signal). `write_flac` still takes a whole buffer (export holds it anyway).
- H-06: capture resampling = `vox_dsp::capture_resample::CaptureResampler` (push-based, primed; never fabricates data ahead — unlike playback's pull-based `StreamResampler`) on the capture-writer thread; `record_start` resamples instead of refusing a rate mismatch; `RecordingResult::sample_rate_hz` is the document rate. A resampler flush zero-pads up to one input chunk of tail — trim it in frequency/golden measurements. `RecordingService` reads the default format fresh from `SettingsStore` per call. `Session::discard_take` also closes a finished-but-uncommitted take. Borrow tip: copy data out of an `rtrb` read chunk before calling `&mut self` helpers.
- H-09: normalize is a job that WRITES BACK (`src-tauri/src/normalize.rs::NormalizeService`, commands `edit_normalize_{peak,lufs}_{start,cancel}`, event `normalize_result`, `JobKind::NormalizePeak/NormalizeLufs`). Pattern for write-back jobs: `DocumentService::begin_normalize_job` takes a read-only source + sets `normalize_busy` → plan off-lock (`plan_normalize_peak/lufs` with `CancelToken`) → `finish_*` re-locks and commits only if the snapshot (`Arc::ptr_eq`) and session id are unchanged. Audio edits + undo/redo refuse with `error.document_busy` while busy. `scan_peak_accelerated` uses the chunk pyramid (`ChunkPeaks::overall()`, `has_non_finite` in the formerly reserved header word). Settings gained `normalize_dialog` — full `Settings` literals in tests need it. Not done: AC-9 wall-clock bench, AC-13 rack-active notice.
- H-10: a failed take commit swaps in a fresh empty session (broken session left on disk for recovery) instead of leaving the take open. Dropouts: RT gap detection compares each block's own (unmapped) `ts.capture_ns` (not `to_app_ns`) → `GapEvent` SPSC ring → capture writer splices silence + `DropoutMark`s → "Dropout N ms" markers committed with the take; `RecordState::dropout_count` live. Test with `FakeEvent::InputDropout(stream_id, frames)` + `CallbackSizes::Fixed(n)`. `LivePeaks` decimates at 65 536 buckets (UI uses the response's `samplesPerBucket`). New fields on shared DTO structs → update every hand-written literal in Rust + TS tests (cross-branch merges hit this: e.g. a Settings literal from one ticket missing another ticket's new field).
- H-03: TP limiter ceiling is a construction guarantee (each sample's gain covers both adjacent intervals); latency = L + 17 (`REQUIREMENT_DELAY`), 257 at 48 kHz/5 ms (SPEC-017 Amendment 2); param timing: ceiling at k+L+1, input gain/release at k+latency. Full SPEC-017 matrix runs in `just test-big`. Generic module telemetry: `RackHost::read_telemetry()` + `SlotInfo.telemetry` → binary `VXMT` frame per control tick (only while subscribed) → `module_telemetry_subscribe` → UI `slotTelemetry(uid)`/`onModuleTelemetry`, `GainReductionMeter` in the slot header (limiter, Dynamics, Noise Gate). Telemetry handles are per INSTANCE — refresh in `replace_with` (unlike curve/NR handles). `loadRack` calls `module_telemetry_subscribe`; `RackSlotDto` test literals need `telemetry: []`. `vox_rack::offline::render` no longer panics when the latency flush starts past the input end. VXMT/`param_set_plain`/job kinds recorded in ADR-003 Amendment 3 + ADR-005 Amendment 1 (2026-09-14). Still pending: align the limiter's telemetry key to `module.<id>.telemetry.<key>`.
- T-204: spectrogram = `vox_dsp::spectro` (frame math, quantizer, Auto FFT) + `vox_engine::spectro` tile service (own worker threads, LRU cache ≤ 25 % of the memory budget, content-keyed so edits recompute only touched tiles, weak ref to the chunk store) → binary `VXST` frames (`PREVIEW`/`LAST` flags) via `spectro_attach/detach/request`. UI: `ui/src/lib/spectrogram/{vxst.ts,geometry.ts,spectroRequester.ts}` — `createSpectroRequester(viewId, { onTile })`, `setAudioRev` on `document_changed`, `request(viewport)` on every view change (cheap), `tile(fft, hop, k)`; `tile.data` is frame-major (`data[f*bins+b]`). `cargo test -p vox-engine spectro` skips tests/spectro.rs — use `--test spectro`; bless the golden tile with `POWERVOICE_BLESS=1`. Deferred: worker cap during save/export, `spectro_stats` IPC, fake-backend underrun test under tile load, SPEC-007 §4.5 span wording amendment.
- H-11: `vox_project::disk::{FreeSpaceProvider, SystemFreeSpace, FixedFreeSpace}` (statvfs / `windows-sys` `GetDiskFreeSpaceExW`, feature `Win32_Storage_FileSystem`); `EngineConfig::disk_space`/`record_volume`, polled ~1 Hz on the control thread → `StopReason::DiskFull` (graceful, keeps the take), `RecordStateDto.disk_remaining_s` (UI amber < 10 min + `LowDiskDialog` confirm). RT dropout detector: > 2 s gap → `StopReason::InputLost` (A-011); gaps are spliced on resampled takes too. `FakeEvent::InputDropout` also pushes the next callback's arrival time forward — run the fake well past the gap. SPEC-004 housekeeping doesn't exist yet (gate it on recording when it lands). The New Recording dialog was never mounted until H-11 (now mounted in App.svelte).
- T-207: spectral pane = `ui/src/lib/spectrum/freqAxis.ts` (log/linear, the future shared SPEC-007 §2.10 module — distinct from `eq/freqAxis.ts`) + `ui/src/lib/spectrogram/{colormap,sampler,hoverFormat,fftLimit,SpectralView.svelte}` + `state/spectral.svelte.ts` (Shift+D toggles). Canvas2D renderer (WebGL2 → H-13). `WaveformView`'s `startSample`/`samplesPerPixel` are `$bindable`; `EditorView` owns the shared viewport and binds both panes. `sampler.ts::sampleGridSpan` is THE max-or-interpolate rule — reuse it. A non-`<div>` root with `bind:this` needs `HTMLElement` typing. Follow-ups → H-12.
- T-202: import = `vox_io::decode` (symphonia 0.6.1, `default-features = false`, features wav/pcm/flac/ogg/vorbis/mp3/isomp4/aac per ADR-007 Amendment 2; MPL-2.0 confirmed) + `vox_io::downmix` + `vox_project::{probe_for_import, import_file}` (progress callback + `CancelToken`); `DocumentService::open` imports any format with average downmix; `document_probe` returns channel labels/peaks for the future T-209 channel-choice dialog. Open dialog accepts .wav/.flac/.mp3/.m4a/.ogg. Rejected cleanly: MS-ADPCM, ALAC, Opus, chained Ogg, RF64. symphonia 0.6 API ≠ 0.5 docs (`copy_to_vec_interleaved` RESIZES the dest; `Packet.track_id` is a field); MP3 gapless trim is built in; M4A elst/iTunSMPB delay NOT applied. hound 8-bit writes expect signed i8. Import is still a synchronous command (job UI → T-209).
- H-14 (fixed): the FLAC "frame number does not increase correctly" / symphonia `UnexpectedEof` bug was STREAMINFO `min_block_size`/`max_block_size` — `flacenc::Stream::add_frame` folds the short last frame into min; decoders then treat the stream as variable-blocksize. Fix: `stream.stream_info_mut().set_block_sizes(BLOCK_SIZE, BLOCK_SIZE)` after `encode_with_fixed_block_size`. `write_flac` now verifies before rename (decode via symphonia, compare to the quantized samples; `IoError::FlacVerifyFailed`). Debug trick: `ffprobe -show_packets` for real frame positions.
- T-107: monitoring = `vox_engine::monitor` (`MonitorOut`, `MonitorLink` packed atomic stamp, `StreamLatency`, `target_frames`, `readout_ns`) + `drift::DriftServo` (PI on ring fill, τ 0.5 s, ±1000 ppm) + `vox_dsp::async_resample::MonitorResampler` (rubato Async, one per output stream, rates 1/8…8, no alloc on relink). Fill is phase-compensated (`F + r_in·(now − stamp)` + 4-frame lookahead). F* = max in period + max out period + 1 ms; monitor ring 65 536 frames (ADR-002 said 8 192 — amendment pending). Through-rack uses the ONE shared playback rack (SPEC-002 OD-1 A); transport restart skips rack reset while monitoring. rubato's `log` feature must stay OFF (`trace!("{:?}")` allocates in `process_into_buffer`). Long drift sims: `FakeBackend::take_recorded_output` + `frame_time_ns`. Svelte 5 synthetic `change` events need `{ bubbles: true }`. Literals: `Settings.monitor_hint_shown`, `RecordStateDto.monitor_latency_us`/`monitor_dropouts`. Dry is no longer bit-exact (resampler always on). ADR-002 Amendment 1 records ring size, variable chunks, phase compensation and the input-latency reading of SPEC-002 §4.4 (2026-09-14). UI doesn't show monitor dropouts yet; `Transport.playing` stays false while recording (module API says true).
- T-306: sidecar = `vox_project::sidecar` (schema v1 `<file>.vo.json`, `extra` maps for forward compat, atomic write + `.bak`, classify None/Valid/Mismatch/Corrupt/TooNew/Unreadable/PartlyInvalid, `crc32-ieee/f32le/v1` fingerprint built from per-chunk CRCs via `crc32fast::Hasher::new_with_initial_len` + `combine`, `persisted_digest` for `sidecar_dirty`). `DocumentService::open` applies rack (`EngineHandle::rack_load_model` → `RackHost::load_model`, works with no output stream)/markers/view by re-flooring; save decides full vs sidecar-only; `DocumentDto.sidecar_dirty` (UI `*` = dirty || sidecar_dirty) + `spectral_view`. Spectral settings are per-document via `sidecar_view_set_spectral`. Recent files in Settings (cap 10, canonical-path dedup), `recent_files_get/remove/clear` + `recent_files_changed`; second instance: `gc::find_already_open` (needs `SessionConfig.source`) → `IpcErrorCode::NeedsConfirmation` → `ConfirmDialog`. Marker `kind` isn't in the core `Marker` (bridged by `MarkerMetaTable`; real field → T-303). Follow-ups → H-12 (waveform view), H-15.
- T-208: analyzer = `vox_dsp::analyzer` (BH4 FFT, 1/24-oct `BandTable`, Fast/Medium/Slow power EMA) + `vox_engine::analyzer` (`AnalyzerTap` RT push gated by `ANALYZER_ON`, tapped at the same `rack + dry` signal as the output meter; `AnalyzerPublisher` runs inline on the control tick like VXMT and fans out per-subscriber responses) → `VXSA` per SPEC-007 §4.9 (header 48, RESET/DROPPED/SILENT — ADR-003 Amendment 4 supersedes Amendment 1's table); peak hold is UI-only. `attach_ring` only in `build_output`'s `Ok` branch. rtrb 0.4 `write_chunk(n)` needs `T: Default`, errors instead of partial — cap `n` at `slots()`. Fake replug needs two `poll_devices()` while absent before plug. `assert_no_alloc`'s eprintln around unplug/replug is a known harmless artifact — trust `fake.rt_violations()`. `Settings.telemetry_rate_hz` 30 Hz is wired to nothing → H-16.
- H-12: `Settings.spectral_defaults` (A-014 app-wide defaults; per-document values stay in the sidecar). Shared viewport lives in `ui/src/lib/state/waveformView.svelte.ts` (get/set accessor pair so `bind:x={store.x}` works across sites), persisted with selection/cursor via `sidecar_view_set_waveform` (debounced, never marks dirty); restore-on-open is a pending restore keyed by rate:length consumed by WaveformView's zoom-to-fit once the width is known (out-of-range spp → zoom full). EditorView owns the ruler + scrollbar (ruler → waveform → divider → spectral → scrollbar). Spectral HiDPI math: `spectrogram/geometry.ts::frameColumnBounds`. Test gotchas: mock `transport_seek` (a `null` reply corrupts the transport store); wrap assertions in try/finally around `unmount` or leaked components break later tests.
- T-301: recovery = `vox_project::recovery` (journal replay from the last checkpoint, stop at the first bad record, CRC-check referenced chunks, fall back to the newest intact state, truncate the journal after the kept record) + `Session::recover` (interrupted take: `apply_open_take_from_wav` / `open_take_as_new_session` / `discard_take`); budget = `vox_project::budget` (compact at ≥ 25 % unreachable, else drop oldest undo — journaled `drop_undo` — then compact; never while recording). Chunk ids are never reused; undo labels carry params (journaled). src-tauri: `recovery_list/recover/discard`, `storage_info`, `document_close`, `housekeeping.rs` thread (rack/view to journal every 2 s, disk check 10 s + after edits). After compaction `Session::store()` is a NEW store → call `set_document` again; new session-creation paths must hold `DocumentService`'s `session_dirs` lock. `fs_util::pid_is_alive` (kill(pid,0) ESRCH isn't `NotFound` in std — fixed a sidecar bug). Process-spawning tests must live in their own test binary (fork copies fds → `flock` locks leak into children). libtest `--test-threads=1 --nocapture` prints `test name ... ` without a newline — match child-process markers anywhere in the line. Literals: `DocumentDto.recovered`, `HistoryStateDto.{undo,redo}_label_params`. Follow-ups → H-17.
- H-16: the control tick is a fixed 60 Hz (`control::TICK`); `Settings.telemetry_rate_hz` (60/30) only gates publishing via `TelemetryRateGate` (divisor counter) for VXTM/VXMT/VXSA; `Control::set_telemetry_rate_hz` also re-derives the analyzer EMA τ; applied at startup and live from `settings_set`. `ManualEngine::tick()` = one full control tick (a "simulated second" = 60 ticks). VXMT/VXSA publishers no-op until an output device is open (tests: `fake.plug` + `poll_devices`). Analyzer prefs in Settings (`analyzer_visible/response/peak_hold`); math in `analyzer/analyzerMath.ts`; real output-device status store. "Menus" are flat toolbars (no dropdown component) → H-19.
- T-209: `document_open` is now an import job (`JobKind::Import`, `document_open_cancel`; cancel leaves the previous document untouched); multichannel files → `NeedsChannelChoice` error → `ChannelChoiceDialog` (per `Settings.multichannel_policy`: ask/average/first); Save As offers WAV/FLAC (`SaveContainer`, `save_snapshot_flac`); clip pre-flight `overs_check` (pyramid-accelerated via `scan_peak_accelerated`) → `ClipPromptDialog`; lossy source → Save redirects to Save As. Sidecar identity must use `saved_file_crc32` (hash of what a later open DECODES — WAV via read_wav, FLAC via DecodeSource), never the pre-quantization f32. Frontend job listeners attach only from `init*()`/action entry points, never from shared helpers like `openDocument()` (breaks tests via clearMocks ordering). `docs/checkpoints/M2-smoke.md` has a manual smoke checklist. Gaps → H-20.
- H-13: renderers live behind `ui/src/lib/render/` (`rendererMode.ts` choose/fallback, `glContext.ts::GlContextHost` owns a canvas's WebGL2 context and latches to Canvas2D forever on `webglcontextlost`, `quads.ts`, `overlayGeometry.ts` shared overlays, `glProgram.ts`). Waveform: `waveform/webglGeometry.ts` + `webglRenderer.ts` (shared `coords.ts::columnYRange`). Spectrogram: `spectrogram/webglRenderer.ts` (R8 tiles width=bins/height=frames sampled with `texelFetch(ivec2(bin, frame))`, 256×1 LUT, uniforms) + `shaderSampler.ts` (≤16-tap TS mirror parity-tested vs `sampler.ts`); samples stay within one tile across 256-frame boundaries (narrow approximation). Override store `state/rendererPref.svelte.ts` (Settings field + View menu → H-19). Gotchas: mutating a module `$state` store synchronously inside an `$effect` body can re-trigger it (`effect_update_depth_exceeded`) — defer with `queueMicrotask`; jsdom canvas is a real EventTarget (stub only `getContext`). Owner smoke checklist on WebKitGTK is in the H-13 report (both panes via WebGL2, zoom smoothness, instant recolor, FFT gating, forced context loss → Canvas2D + one notice, DMA-BUF disabled default).
- H-19: real menu bar = `ui/src/lib/menu/*` (MenuBar, MenuItemRow/Checkbox/Radio/Separator, `menuKeyboard.ts` — direct-children query + `stopPropagation` so nested popups never double-handle, `menubar.svelte.ts` module `$state` coordination via `data-menu-trigger`/`data-menu-popup`, `mnemonic.ts` Alt+letter) + per-domain menus (Document/Edit/View/Effects/Help; Recent Files, Markers, Favorites, Renderer submenus; About dialog). Shortcut labels from `keymap/shortcutLabel.ts` (reads `DEFAULT_KEYMAP`, platform-aware). Items with a keymap binding go through `dispatchAction()`. `Settings.renderer_preference` (Auto/Webgl2/Canvas2d) backs `rendererPref.svelte.ts`. Recent Files lost the per-entry "×" (H-15's missing-file dialog restores Remove). Test flake note: after a normalize-favorite click, don't `setTimeout`-wait; make the `mockIPC` catch-all throw.
- T-304: record operations (Insert/Overwrite at cursor, Punch with pre/post-roll) = `vox_engine::record_op` (SPEC-022 §2.2 rules) + control-thread phase machine (pre-roll → recording → post-roll → committing) → `record_prepare` → `DocumentService::begin_record_op` → `record_start_op` → done callback → `commit_take_op` (`vox_project::commit_take_window`, equal-power fades rendered into new chunks). EVERY new end path must call `op_try_seal` or the capture writer never finishes and shutdown hangs. `RecordWindow` is journaled off the control thread (`RecordingService::note_window` spawns). During an op heard positions are virtual (`q + offset`). Latency offsets in `Settings.record_offsets` (per host/input/output/rate), panel prefs in `Settings.record`; calibration = `vox_dsp::calibration` sweep + analysis, `calibration_run/cancel` job + `calibration_result` event (needs in/out at the same rate). Fake backend `Loopback` (needs `record_output`; bit-exact at 0 dB; `residual_ns` = hidden latency). `InputEvent::Block.take_frames` includes dropout fills. Runtime-built i18n keys go through `tDynamic`. Clippy `float_cmp` in tests → compare `to_bits()`. Follow-ups → H-21.
- H-15: `SlotModel` (crates/rack) has a public `raw: Option<Value>` + hand-written Serialize/Deserialize — a malformed slot becomes an "Unreadable module" placeholder instead of invalidating the rack; new `SlotModel { .. }` literals need `raw: None` (use `SlotModel::new`). Recent files: `RecentFileDto::from_entry(entry, exists)`, `recent_files_get` checks existence off-thread with a shared 300 ms budget; missing entry → `RecentMissingDialog` (Locate / Remove / Cancel); menu rows can be `muted`. Read-only folder: `check_folder_writable` (probe file) before any save byte → `error.save.permission`. Menu rows must be DIRECT children of the popup (`menuKeyboard.ts`) — no wrapper divs with extra buttons. en.json has pre-anticipated keys from spec tickets — check before adding strings. Sidecar crash test in `crates/project/tests/crash.rs`; perf guard `tests/sidecar_perf.rs`.
- T-406: presets = new crate `vox-presets` (`ModulePresetStore`/`RackPresetStore`, atomic JSON under `<config dir>/presets/{modules/<id>,racks}/<name>.json`, `.tmp-<pid>` files ignored, `sanitize_preset_name`/`sanitize_module_id`, `factory_rack_presets()`); built-in factories expose `presets()`; `RackHost::apply_module_preset` (blob → replacement crossfade; no blob → smoothed params, ADR-005 §12) / `reset_to_default`; `RackCommand::ApplyModulePreset/ResetToDefault`; `RackSlotDto.module_id` (typed TS fixtures need it). Factory racks: Podcast voice (limiter −1 dBTP), Audiobook (ACX) (−3 dBTP), Gentle cleanup. Loading a rack preset confirms whenever the rack is non-empty (rack edits aren't undoable in v1). Rename/overwrite/manage UI → H-22.
- H-17: measured on a real disk — AC-3 worst undo/redo on 20 000 pieces 1.39 ms (limit 50), AC-11 recovery of a 60-min/1000-record session 63.6 ms (limit 5 s) → no delta checkpoints needed (in `just test-big`). Compaction = `begin_compact` (short) → `PreparedCompaction::copy_chunks` (no session access, document mutex RELEASED) → `finish_compact` (copies the delta, reserves new-generation ids from the chunk count read THERE — not in begin — or ids collide). Journal `take_marker` + `Session::note_take_marker` restore markers of an interrupted take (live M-during-take wiring is H-21). `Notice.cleared` (Rust `#[serde(default)]` but TS required → every Notice literal needs it). Preferences dialog: `ui/src/lib/preferences/*` (Storage section; add sections there). Recovery dialog stays open for remaining sessions (A-015). `crates/project/tests/common/mod.rs::sweep_finished_runs` sweeps `/tmp/vox-project-*`.
- T-705: packaging via `src-tauri/tauri.conf.json` (file associations wav/flac/mp3/m4a/ogg, deb depends/recommends, AppImage); `just build` → `target/release/bundle/{deb,appimage,rpm}/`. On this rolling-release host the AppImage step needs `NO_STRIP=1` + a `PKG_CONFIG_PATH` gdk-pixbuf stub (upstream linuxdeploy bugs; see docs/building.md). `THIRD_PARTY_NOTICES` generated by `scripts/notices/generate.py` (full graph; `just notices`; `--check` runs in `just check` — adding a dependency requires `just notices`); shown in Help → About. `docs/user-guide.md` shortcut table generated from `DEFAULT_KEYMAP` (`just shortcuts-table`). `.desktop` validated in `just build`. Windows/macOS installers are documented (docs/building.md), not built.
- H-21: markers during a take/operation join its single edit (`Session::open_take_markers()`, `place_take_marker`: pre/post-roll markers at the heard position, window markers at `at + k` clamped to committed audio); a cancelled op turns them into separate "Add Marker" entries (`Session::cancel_take`); `marker_add` allowed during takes (rename/move/delete still refused); `markers_get` includes pending markers (UI `isTakeMarker` keeps them unshifted during Insert). Waveform during an op = `ui/src/lib/waveform/opLayout.ts` (live take at `at`, Insert shifts later audio/markers, punch shaded); the H-07 zoom-to-fit view only for a new recording into an empty document. Fake `AlignedTalent` (`FakeBackend::set_talent`, needs `record_output`) for exact punch tests incl. drift; rig `rig_with(doc, src, loopback, mic_skew_ppm)`. `history_state` is emitted after committed/cancelled ops. Preferences → Recording section (offsets forgotten via `settings_set`). `TelemetryFrame` is `Copy` (clippy rejects `.clone()`). Polish → H-23.
- T-801: `crates/sandbox-ipc` (`vox-sandbox-ipc`) = versioned segment layout (control block + position-indexed sample rings + event rings; golden-tested in tests/layout.rs), regions via memfd/shm_open/CreateFileMapping, `Wakeup` trait (Linux futex; portable spin-then-yield), `HostEnd` (no-alloc, bounded wait, splice/linear crossfade bypass), `PluginEnd`, `Monitor` (crash/hang/failed/exited), test plugin binaries `vox-sbx-test-{gain,crash,hang,slow}`, `benches/round_trip.rs`. Design recorded in ADR-008 Amendment 1 + ADR-002 Amendment 2; in `just check-cross`. The agent's final report was lost to a session restart — read ADR-008 Amendment 1 for the protocol and numbers.
- H-20: Save As offers Dither (TPDF/None; `vox_dsp::dither` plain-rounding path) and keeps the source format (FLAC stays FLAC, full WAV promotion table); notices `notice.open.format_mapped`, `notice.save.metadata_dropped`, multichannel-source confirm before the first save. Import shell: `import_started` event (name, rate, length) right after probing → non-modal `ImportProgressBar` + window title "Opening ‹name›…"; the previous document is never mutated until commit (cancel/error need no restore). No `peaks_progress`/streaming-peaks IPC exists despite SPEC-005 §2.3 — live partial peaks during import need it first. New DTO/Settings fields (`save_dither`, `defaultDither`, `dither`, `confirmMultichannel`) → every hand-written TS literal (H-18 will add factories).
- H-24 (owner-reported layout fix): `Settings.layout` (`LayoutPrefsDto`: markers/rack widths, dock height, collapsed flags, `DockTabPref`) persisted via `layout/layoutSettings.svelte.ts` (debounced); splitters = `layout/splitterMath.ts` + `layout/Splitter.svelte` (`role="separator"`, arrows, double-click reset, collapse). App shell = 3 rows; bottom dock has a bounded height with Meters/Analyzer and Loudness tabs. Scale math: `waveform/timeRuler.ts` (adaptive `0:05` / `0:05.250` / `1:02:03`), `waveform/amplitudeAxis.ts` (SPEC-006 §2.4 dBFS ladder; centerline/−∞ unlabeled), `analyzerMath.ts::dbAxisTicks`, `eq/freqAxis.ts::eqFrequencyTicks`, `eq/gainAxis.ts::gainAxisTicks`, `spectrogram/colormap.ts::cssGradientFor`. EditorView's shared ruler adds `RULER_GUTTER_PX` (48) to tick x. Rack/Markers sized via `:global()` from App.svelte. Test gotcha: add `vi.useRealTimers()` to the top-level `afterEach` (a timed-out fake-timer test leaks fake timers).
- T-802: `powervoice-sandbox` binary (`crates/sandbox`: one process per plugin instance, `PluginBackend` trait in `src/backend.rs` — T-803+ add formats there; `test` backend wraps the real Gain + T-801 faults; SCHED_FIFO attempt; exits on Shutdown / stdin EOF / host pid gone / `PR_SET_PDEATHSIG`) + control channel = framed JSON over stdin/stdout pipes (not ADR-008's socket; ADR-008 Amendment 2) + `vox-plugin-host` (`crates/plugin-host`: `ProxyModule` params mirror, events via ring, `HostEnd` process, latency = block B + plugin latency, state wraps format/id/version; `SandboxFactory`; ONE watchdog thread spawns every sandbox so PDEATHSIG holds, polls 10 ms, reaps). Rack policy: fail → 15 ms bypass crossfade + "Restarting" → restart once after 200 ms from the last committed state → second failure "Failed" + reason; manual Retry resets; offline renders abort on plugin failure. `AdapterHealth` extension (ADR-005 Amendment 3). Shared registry `src-tauri/src/plugins.rs`. Dev flag `POWERVOICE_DEV_PLUGINS=1` exposes test:gain/crash/hang. Tests needing the binary live in `crates/sandbox/tests` (`CARGO_BIN_EXE_*`); poll `/proc/<pid>` after dropping a proxy; `pgrep -f` matches itself — use `ps … | grep 'powervoice-sandbo[x]'`. T-803 notes: instance creation is synchronous on the rack control thread (real plugins need async load + a loading status); state blob captured at creation (GUI changes need refresh at save); plugin latency change → `HostRequest::Restart`; still to do: bundle the sandbox binary, Windows job object/named events, rtkit/MMCSS, global failure notice.
- H-25 (design system): tokens `ui/src/lib/theme/design-tokens.css` (`--pv-*`), old names only via `theme-bridge.css` (declared on `:root, [data-theme]` so light subtrees re-resolve); component kit `ui/src/lib/ui/*` (Button, IconButton, Tooltip, ToggleButton, SegmentedControl, Select, Slider, NumberField, Toggle, PanelHeader, Tabs, Badge, StatusDot, Readout, Kbd, Separator, EmptyState, Icon (Lucide), Dialog) — NEW UI MUST USE THESE. Shared kit types in `ui/src/lib/ui/types.ts` (svelte-check rejects `export interface` in component scripts). `Settings.theme`; `theme.svelte.ts`; contrast tests (`contrast.ts`). Dev-only pages: `?gallery` (every component, both themes) and `?preview` (real app on mocked IPC, `src/dev/previewIpc.ts`) — useful for headless screenshots. Rack off-screen cause: a one-column implicit CSS grid grows to its widest child → use `grid-template-columns: minmax(0, 1fr)`; column fitting in `layout/fitColumns.ts`. Vitest turns CSS imports (even `?raw`) into '' unless matched by `test.css.include` (don't end the pattern with `$`). Canvas renderers read colours per draw (theme switch applies on next redraw). Docs: `docs/design/ui-audit.md`, `docs/design/design-system.md`.
- `/tmp` is a 30 GB tmpfs with a per-user quota: store-backed tests preallocate 64 MB+ segments per temp dir, so leaked test dirs make unrelated tests fail with `DiskFull`/"Disk quota exceeded" (EDQUOT). Test temp dirs must be removed or swept — src-tauri `document.rs` tests sweep finished runs' `powervoice-app-doc-<pid>` dirs; follow that pattern (or a drop guard) for new store-backed test helpers. If tests fail with EDQUOT: `du -sh /tmp/powervoice-*` first.
- `.githooks/pre-commit` runs only `cargo fmt --all --check` — run `cargo fmt --all` before committing.
- Merging parallel branches: conflicts are almost always additive lists (ipc/mod.rs command lists, events enum, App.svelte imports, commands.ts wrappers) → union both sides (watch the separator comma / closing brace), take either side of `bindings.ts` then `just gen-types`.
- `cpal` 0.18: no per-channel input selection (open full device, deinterleave), no hot-plug events (poll ~1 s off-thread), input/output are separate streams (monitoring needs drift-corrected ring). Enable `pipewire`/`jack` features on Linux; ALSA headers still required.
- `hound` has no `cue`/`LIST adtl` support → custom RIFF chunk code.
- `ebur128` needs feature `precision-true-peak` for 4× oversampled TP.
- Mono BS.1770: 1 kHz sine at −20 dBFS ≈ −23.0 LUFS (single channel, G = 1.0; stereo −23 dBFS/ch ≈ −23 LUFS).
- WebKitGTK WebGL can be slow (esp. NVIDIA); workaround `WEBKIT_DISABLE_DMABUF_RENDERER=1`. Owner GPU: AMD Phoenix (Mesa).
- Tauri on Wayland: global shortcuts unsupported (we only need in-window), pointer-coordinate quirks reported.
- ReaPlugs are VST2-only (2016), no Linux build.

- (from T-002) tauri-specta is still 2.0.0-rc.25 with exact `specta` pins → we use **ts-rs 12**; generate with `TS_RS_LARGE_INT=number` (u64 → number; positions < 2^53).
- (from T-002) Tauri sync commands run on the main thread → make command handlers `async`.
- (from T-002) cpal `StreamInstant`s are not comparable across streams → map each stream to the app clock inside its own callback.
- (from T-002) rubato 5: `Async` (runtime-adjustable ratio), `Fft` (fixed ratio), `Slip`; `process_into_buffer` doesn't allocate.
- (from T-002) Windows: mmap views and `WriteFile` aren't guaranteed coherent → write through the mapping; preallocate segments or a full disk means SIGBUS.

## Risks
| Risk | Mitigation |
|---|---|
| Windows/macOS untested (owner has Linux only) | `just check-cross`; platform-neutral code paths; flag at each checkpoint |
| WebKitGTK rendering performance | T-007 spike in M0; Canvas2D/CPU-tile fallback |
| LAME LGPL linking | runtime-loaded `libmp3lame` via `libloading`; owner confirmation before M6 |
| VST2 legal exposure | gated ticket T-811; prefer Carla bridge |
| ysfx JIT crashes | confined to `plugin-sandbox` (library is Apache-2.0) |
| AAC decoding patents | legal review before public distribution (symphonia AAC is opt-in feature) |

## Owner decisions at the M0 checkpoint (2026-09-12)
| ID | Decision |
|---|---|
| D-011 | License **MIT OR Apache-2.0** (LICENSE-MIT, LICENSE-APACHE; metadata in T-104) |
| D-012 | **Renamed to PowerVoice** (formerly the working name VoxEdit); app id `app.<name>.editor`, module ids `org.<name>.*` unless the owner gives a domain |
| D-013 | MP3 export via runtime-loaded `libmp3lame` (`libloading`) approved |
| D-014 | Shortcuts: **Shift+Space = Play from start** (plays from selection start or 0); **Record = Shift+R (provisional)**; loop key in SPEC-019. PROMPT §3.6 "Shift+Space record" was wrong |
| D-015 | SPEC-004 OD-1 drop oldest undo steps with notice · OD-2 keep recovery data until discarded · OD-3 accept import cost (< 3 s open on SSD/NVMe only) · OD-4 rack edits not undoable except bake restores the rack (ADR-004 Amendment 1) |
| D-016 | `WEBKIT_DISABLE_DMABUF_RENDERER=1` **on by default** on Linux, opt-out `POWERVOICE_WEBKIT_DMABUF=1` (ADR-009 Amendment 1; T-104) |
| D-017 | Output device lost while recording → **recording continues** (SPEC-001 §2.4 exception, SPEC-002 AC-16) |
| D-018 | **Stop returns the playhead to the play-start position**; Pause keeps it; engine-initiated stops behave like Pause (SPEC-003 §2.1, AC-9) |
| D-019 | Whole-rack A/B is listening-only; exports always render the rack |
| D-020 | Record works with only an input device (SPEC-001 §2.3) |
Deferred to the M8 checkpoint: VST2 via Carla, unsigned native modules, sandboxed-plugin monitoring latency (SPEC-002 OD-1).

## Autonomous decisions (after D-021; owner may revisit)
- A-010 (2026-09-14): free disk space via `libc::statvfs` on unix (already a `cfg(unix)` dep of vox-project) and `windows-sys` `GetDiskFreeSpaceExW` on Windows (already in the tree via tauri/cpal; direct `cfg(windows)` dep allowed) — no new third-party crate like `fs4`.
- A-011 (2026-09-14): input gaps > ~2 s follow SPEC-002 §2.4 (treated as device loss: stop and keep the take), not filled with silence; shorter gaps are filled + marked (H-10).
- A-018 (2026-09-14): first launch uses the Dark theme (pro-audio norm); Preferences → Appearance offers Dark / Light / Match system. Windows dialog button order (primary first) and a shared menu/popover component are a follow-up.
- A-019 (2026-09-15): Match System follows light/dark AND `prefers-contrast: more` → High Contrast (accessibility default; T-708 open question; implemented by H-31).
- A-017 (2026-09-14): UI icons = Lucide via `lucide-svelte` (ISC, in THIRD_PARTY_NOTICES via `just notices`); typography = system font stack (no bundled font). Design work goes through the `ui-ux-designer` agent (`.claude/agents/ui-ux-designer.md`) against `docs/design/design-system.md`.
- A-016 (2026-09-14): no `PostRoll` phase event when the output is lost mid-window (no post-roll plays), and the waveform follows the record head during a record operation like the playhead during playback (H-23).
- A-015 (2026-09-14): the recovery dialog stays open after one Recover/Discard to handle the remaining sessions; no delta-checkpoint design until AC-11 is measured on a real disk (H-17), and only proposed if it misses.
- A-014 (2026-09-14): spectral DISPLAY prefs (colormap, scale, floor/ceiling, FFT size) persist in app `Settings` (H-12); per-document VIEW state (pane visibility, split ratio) waits for the T-306 sidecar. WebGL2 renderers are H-13; Canvas2D stays the fallback.
- A-013 (2026-09-14): the spectrogram tile service keeps its own std worker threads (no rayon) until the engine grows a shared worker pool (ADR-002); the golden VXST tile stays bit-exact, switching to the tolerance variant if another CPU's SIMD path breaks it.
- A-012 (2026-09-14): after the four slices, the owner's founding feature "spectral display" (T-204 tile service → T-207 renderer) jumps ahead of the rest of the full-spec backlog.
| ID | Source | Decision |
|---|---|---|
| A-001 | SPEC-006 (T-200) | Waveform: dots at ≥ 3 px/sample; clip highlight at \|x\| ≥ 1.0; PARTIAL buckets drawn as `--wave-pending`; reject out-of-order `request_id`s as well as stale `audio_rev`; vertical zoom 1×–256×; default amplitude ruler dBFS, time ruler `hh:mm:ss.fff`; zoom-full exact fit; `=`/`-` zoom (playhead-centred if visible), `Alt+=`/`Alt+-` vertical, Ctrl+wheel cursor-centred zoom, Alt+wheel vertical; double-click selects all; smooth follow within a 10–90 % band; zero-crossing snap ±512 samples on selection edges only; handle hit width 6 px; perf AC p99 ≤ 50 ms with ≤ 1 outlier per 10 s sweep. Zoom-to-selection / zoom-full keys unbound until SPEC-019. |
| A-009 | SPEC-014 (T-500) | NR **`org.powervoice.noise-reduction`**; Capture Noise Print **Shift+P** (provisional), NR panel Ctrl+Shift+P; capture targets last-focused NR slot → first NR slot → inserts one at the top; capture source = audio as the NR slot receives it (earlier slots rendered offline, 1 s pre-roll, placeholders skipped); selection ≥ 0.5 s (warn < 1 s, analyse ≤ 60 s, error on silence, warn > −35 dBFS RMS, disabled while recording); profile = 8192-point mean power density + log-power spread per bin, 32 824-byte versioned blob, converted to any FFT size/rate; decision-directed Wiener (β 0.98) + dB-domain frequency/time smoothing; latency = N (42.7 ms at 48 kHz, FFT 2048 default; 1024–8192 offered, > 2048 warns about PROMPT §4's 50 ms); params Reduce by 0–40 (12) dB, Noise reduction 0–100 (100) %, Output noise only, FFT size, Sensitivity −6…+12 (+3) dB, Spectral smoothing 0–1000 (100) Hz, Attack 1–200 (5) ms, Release 10–2000 (100) ms; params take effect at frame boundaries (all `allow_delayed_effect`); no profile → bit-exact passthrough keeping latency; presets carry the profile; Output noise only = input − processed, saved state with a badge + export/bake confirmation; musical-noise metric = kurtosis ratio ≤ 1.5 (naive subtractor must score ≥ 3.0; calibrate in hardening); CPU ≤ 2 % (defaults). NR output is bit-identical only on the same machine (FFT SIMD paths) → golden tests use tolerances. `realfft` added to `vox-dsp`. |
| A-008 | SPEC-015/017 (T-400) | EQ **`org.powervoice.parametric-eq`**: fixed band roles HP, LowShelf, Peak 1–5, HighShelf, LP; HP 80 Hz / LP 12 kHz default off, others 0 dB → default EQ bit-exact; gain ±24 dB; peak Q 0.1–30 (1), shelf Q 0.3–2 (0.707); HP/LP 6–48 dB/oct Butterworth cascades; **f64 coefficients + state**; 20 ms ramps (log freq/Q, dB gain) with per-sample coefficient recompute while ramping; band on/off + slope changes crossfade 20 ms; disabled bands pass bit-exact; freq clamped to 0.49·fs; tail ~34.7 s worst case. Graph: `set_param_plain` drag command, `VXRC` curve frame, ±12 dB axis (±24 option), post-rack analyzer behind, wheel = Q. Swept narrow/strong band jumps are exempt from §4.3 (physics). TP limiter **`org.powervoice.true-peak-limiter`**: input gain −12…+24 (100 ms smoothing), ceiling −12…0 dBTP (−1.0, 20 ms), release 10–1000 ms (100, 12 dB per release time), look-ahead 1–10 ms (5, restart on change); 4× oversampled detection (128 taps) + parabolic refinement, latency = look-ahead + 16 samples; hold + linear-dB release + two moving averages → under ceiling by construction, bit-identical below ceiling; measured against a new testkit **16× true-peak reference** (ceiling + 0.10 dB), ebur128 cross-check + 0.25 dB. |
| A-007 | SPEC-013/016 (T-400) | Shared dynamics engine (SPEC-016 §4): 5 ms sliding peak / 20 ms RMS detector, τ = 63.2 % in the dB gain domain, gate opens with a raised-cosine ramp of exactly the attack time and closes exponentially, 20 ms parameter ramps + crossfaded enables, look-ahead 0–20 ms (restart on change). Dynamics `org.powervoice.dynamics@1.0.0`: order AutoGate → Expander → Compressor (+makeup) → Limiter; knee width default 6 dB; manual makeup; Peak/RMS for Expander/Compressor (default RMS); only the Compressor on by default (−20 dBFS, 3:1, 10/100 ms). Noise gate **`org.powervoice.noise-gate@1.0.0`** (ADR-005 §2 id): threshold opens, closes at threshold − hysteresis; peak detection; range default −30 dB (to −∞); sidechain HPF 24 dB/oct on at 100 Hz; defaults −40 dBFS, 6 dB hysteresis, 2/50/100 ms. **Slice 3 follow-ups:** ADR-005 gets a `TransferCurve` extension and a `set_param_plain` command (shared with SPEC-015); ADR-003 gets `VXTC`/`VXMT` frames; modules write initial telemetry in `activate`/`reset`; all params except makeup need `allow_delayed_effect`. |
| A-006 | SPEC-022 (T-300) | Record behaviour: empty doc → new recording; non-empty selection → **punch-in** (setting can switch to "record at selection start"); otherwise at the cursor in the current mode. Default mode **Insert** (Overwrite via record-panel toggle / right-click Record). Pre-roll **5 s** (0–20 s, Audition parity, silence-padded near file start); post-roll **1 s** (0–20 s); punch-and-roll pre-roll for cursor recording optional (off). Punch replaces exactly the selection and auto-stops at its end; Stop in pre-roll cancels (no edit), in recording keeps a partial punch to the heard stop point, in post-roll = full punch; Record while playing stops first. Crossfades equal-power **10 ms** (0–50 ms) inside the replaced range (outside bit-identical) for Punch/Overwrite, none for Insert. Original audio muted during recording ("Hear original" option off). Markers stay put for Overwrite/Punch, shift for Insert. After punch the selection stays (Shift+R re-punches); after insert/overwrite no selection, playhead at the end. Recording offset signed (positive = late), ±500 ms, per (host, input, output, rate), applied only when recording follows playback; calibration = 5 log sweeps after the rack with monitoring forced off, ≥ 4/5 agree, Verify pass. Output lost in pre-roll cancels; in recording continues; input lost keeps a partial punch. Punch refused without an output device. Undo labels "Record"/"Punch-in"; selection locked while recording. |
| A-005 | SPEC-009/018 (T-300) | Markers: `{id,pos,len,name,kind}` (User/Dropout/Unknown), sorted by pos then id. **M** adds (stopped + selection → region; playing/recording → point at heard position from `KeyboardEvent.timeStamp`, key repeat ignored); names "Marker 01…" (max+1); rename `/`, F2, double-click; drag after 3 px, snap 6 px to cursor/selection/markers (Alt off), Shift-drag moves region, Esc cancels; **Ctrl+0** delete selected, **Ctrl+Alt+0** delete all (match digits by physical key), no confirmation (notice with Undo); Delete key follows focus (panel = markers, waveform = audio); next/prev **Ctrl+Alt+→/←** (no wrap; "previous" skips markers < 0.5 s behind while playing); Markers panel left dock 300 px, virtualized, numeric-aware sort; limits 10 000 user / 100 000 total; open precedence: sidecar wins unless the WAV cue differs from the sidecar's copy (then WAV wins with a notice). Sidecar: file name = full name + `.vo.json` (`x.wav.vo.json`); written only on Save/Save As; sidecar-only saves skip audio re-render; `sidecar_dirty` by content; identity = rate + length + CRC-32 of decoded/written samples; no-sidecar open keeps the current rack (chapter workflow); schema v1 deterministic with `version` + `compat_version`, in-memory migration; `.bak` (1 generation) before overwriting unreadable/other-version sidecars; changed-on-disk confirmation; autosave = session journal only; open-in-another-instance warning (Open Anyway/Cancel); recent files 10, no pinning, missing greyed + Remove; spectral settings per document. Format id `org.powervoice.sidecar` becomes permanent at T-306. |
| A-004 | T-102 open questions | Input dropdown hides PipeWire sink monitors and `default_sink`; `default_input`/`default_output` shown as "System default" (T-109). ALSA's slow first enumeration → two-phase diff (names first, capabilities later) as a follow-up; acceptable because PipeWire is the default host. PipeWire offering only 48 kHz is acceptable (reader resamples). |
| A-003 | SPEC-005/007 (T-200) | Import: extra WAV variants (8-bit, 32i, 64f, A/µ-law) open with a notice; ADPCM/AIFF/Opus/ALAC/MKV rejected. Stereo → mono **average** by default (LFE excluded) or pick a channel, dialog at open with Ask/Always mix/Always first; silent-channel preselect; identical channels skip the dialog; first save over a stereo original warns. Save format per document (8-bit/law → WAV16; 32i/64f → WAV32f; compressed → Save As WAV24). TPDF dither by default, skipping exactly-representable 4096-sample blocks (bit-exact open→save, silence stays silent), seeded; no noise shaping. Clip prompt only when a sample > 1.0 ("Clip and save" / "Save as 32-bit float"). WAV: PCM format tag for 16/24, `cue `+`LIST adtl` after `data`, UTF-8 names (CP-1252 fallback on read), regions via `ltxt` `'rgn '`, no RF64 writing; other metadata dropped with a notice; FLAC no markers. FLAC block 4096 + decode-compare before replace. Export resampling f64, ±0.1 dB passband to 0.9×Nyquist, aliasing ≤ −100 dBFS. New CLI `convert`/`markers`. Spectral: hidden by default, Shift+D split 50/50 below the waveform; log axis 20 Hz–Nyquist; floor −120 / ceiling 0 dB; Inferno (Viridis, Grayscale); FFT Auto ≈ 2048×rate/48 k; zoom-dependent hop with power averaging; PREVIEW tiles; tile memory 25 % of budget (Rust) + 192 MiB GPU. Analyzer: post-rack tap (same as output meter), FFT 8192 Blackman-Harris → 1/24-octave bands, Fast/Medium/Slow 50/150/500 ms (Medium), peak hold 2 s then 12 dB/s, bottom dock, `VXSA` at 60 Hz. |
| A-002 | SPEC-008/010 (T-300) | Editing: cut/copy/delete/trim/silence disabled without a selection; paste/insert silence at the cursor; insert silence with a selection inserts at its start; pasted/inserted audio becomes the selection; trim → cursor 0; undo/redo clears the selection, playhead to the first affected position; markers never deleted by audio edits (region collapsing to 0 → point marker), trim clamps; **no micro-fades**; one in-app audio clipboard that survives document switches (materialized on close), rate-converted on paste with a notice; ops don't re-snap; revision-guarded commands; `clipboard_changed` event; splice ≤ 5 ms / command ≤ 50 ms at 20 k pieces. Keys: Ctrl+X/C/V, Delete, Ctrl+T trim (Crop). Normalize: sample peak only (true-peak normalize in M6), no DC adjust, target −60…0 dBFS or %, no-op below −120 dBFS or |gain| < 0.001 dB, non-finite input refused, playback stops at command start, favorites as toolbar buttons + Favorites menu, no default keys, one-time rack notice per session, 60-min normalize ≤ 10 s. |

## Open questions for the owner (M0 items below were resolved — see the table above)
- Project license (none chosen yet — private).
- Final product name (working name "PowerVoice").
- (ADR-004) Session disk budget exceeded (proposed max(8 GiB, 8× doc), keep ≥ 2 GiB free): auto-drop oldest undo steps with a notice, or warn only?
- (ADR-004) Recoverable sessions: keep until the user discards them (proposed), or auto-delete after N days?
- (ADR-004) Opening a file imports it into the session store (~1.33× file size on disk for 24-bit). Acceptable? Slow HDDs may miss the < 3 s open target.
- (ADR-007) Confirm LAME via runtime-loaded `libmp3lame` (bundled as a separate replaceable library on Windows/macOS/AppImage; system dependency on deb/Arch). Needed before M6.
- (ADR-007) Project license: MIT OR Apache-2.0 (recommended) vs GPL-3.0-or-later (GPL-2.0-only is incompatible with Apache-2.0 deps).
- (ADR-008) VST2 via Carla bridge (T-811): approve or reject.
- (ADR-006) Module id namespace `org.powervoice.*` + app identifier `app.powervoice.editor` become permanent at M3 (first sidecar) — decide together with the product name.
- (ADR-006) Allow installing unsigned native modules? (sandbox isolates crashes, not malice)
- (SPEC-003) Audition default shortcuts: Space = play/stop and Home = return to start are verified; **Record (PROMPT says Shift+Space) and loop-toggle bindings are unverified** (one source says Shift+Space = "play from start"). Owner knows Audition — confirm Record/loop keys. Also: adopt Audition's "return playhead to start on stop" preference (Shift+X)?
- (ADR-009) Make `WEBKIT_DISABLE_DMABUF_RENDERER=1` the default for Linux (`just dev` and packaged launcher), with an opt-out? Also: run the ADR-009 §6 manual input checklist (`just spike`, focused window: Space, Shift+Space, Ctrl+Z, Ctrl+Shift+Z, drag coordinates).
- (ADR-008) One extra block of latency per sandboxed plugin (~5 ms at 256 frames) when monitoring through the rack — acceptable?

## Orchestrator follow-ups
- ~~Reconcile ADR-005 ts-rs with ADR-003~~ — done by T-003 (no ts-rs in `module-api`).
- ADR-005 open question "are rack edits undoable?" vs ADR-004 journal (rack state currently outside undo history) — decide in SPEC-004/SPEC-012.
- Verify ADR-007's table covers rayon, memmap2 0.9, libc, assert_no_alloc 1.1.2, ts-rs 12.0.1 (crc32fast/rayon/basedrop added by T-003).
- M8: ADR-002 RT rules need a narrow documented exception — the sandbox proxy's non-blocking wake is a syscall on the audio thread.
- PROMPT §3.7 wording (`prepare`, `tail_samples()`, "documented per adapter" dual-mono) is superseded by ADR-005 (D-008) — mention at M0 checkpoint.
- T-007 must also verify: raw channel/`Response` payloads arrive as ArrayBuffer on WebKitGTK; 30 vs 60 Hz telemetry cost; cpal `playback` timestamps meaningful on PipeWire; opening devices at document rate doesn't disturb other apps.
- **From SPEC-015/017 (T-400):** ebur128's true peak reads **−0.20…+0.20 dB** depending on content (and only 2× oversampling at 96 kHz) — never use it as the normative TP meter; use the testkit 16× reference. Plain 4× TP detection under-reads up to 0.26 dB on limiter output (parabolic refinement fixes it). Modules with latency must delay parameter events internally (ModuleTestHost's latency-aware check forbids reacting before event + latency). EQ ModuleTestHost runs need `allow_delayed_effect` for every band param. **`VXSA` analyzer frame is described differently in SPEC-007 §4.9 vs ADR-003 Amendment 1 (header 48 vs 32) — reconcile when implementing the analyzer (before the EQ graph uses it).** Slice 3 ADR follow-ups now also include `VXRC` (EQ curve frame); `set_param_plain` shared by SPEC-015/016. T-602 must cap selection-bake tails (EQ reports ~35 s).
- **SPEC-017 (TP limiter, M4 W0):** `ebur128` true peak reads ~+0.10 dB high near fs/4 (−2.886 dBTP vs analytic −2.990). A "ceiling −1.0 → pass if ≤ −0.9 dBTP" criterion leaves no margin for meter error — characterise the meter (or use an independent higher-oversampling reference in testkit) and set tolerances accordingly.
- **SPEC-000 cross-doc notes (T-008):** resolved by orchestrator — (1) CLAUDE.md now says "return ring", not basedrop; (2) ADR-002 capture overflow = stop + finalize at last good sample (matches ADR-004 §7.4); (3) ADR-002 input opens only when armed/recording, not merely monitoring ≠ off; (4) ADR-004 crash bound = ≤ 250 ms process crash / ~1.5 s power loss; (6) step-derived decimals already implemented in T-005 review round. Open: (5) bake undo restoring the pre-bake rack needs an opaque rack-state attachment per undo entry → ADR-004 amendment only if the owner accepts SPEC-004 OD-4. Still to update in SPEC-000 once its author finishes: TELEMETRY_RATE = 60 Hz (ADR-009), ADR-009 no longer "pending", note 6 resolved. SPEC-001 §2.3 amended: Record available with input device only (SPEC-002 open Q2).
- **Spec consistency pass:** ✅ done by orchestrator for SPEC-001 (PipeWire default host on Linux, ALSA fallback) and SPEC-003 (Stop = Pause in v1 pending the owner's return-to-start decision; AC-4 now sample-exact loop concatenation + reset-at-seam). Still to do when the T-008 Opus half lands: check SPEC-002 treats the Record shortcut as unverified like SPEC-003, and cross-check SPEC-000/002/004/012 against SPEC-001/003 and the ADRs.
- **From SPEC-022 (T-300):** T-301 reserves journal records `take_window`/`take_cancel` and the per-edit `MarkerMapping` (ADR-004 Amendment 4). T-106 capture writer needs ~2 s look-back. T-304 extends `FakeBackend` with a loopback source, a perfectly timed "talent" source and hidden unreported latency. ADR-003 Amendment 2 adds `record_phase`/`record_finished` events. SPEC-006 gains a selection lock while recording. Gotcha: equal-power crossfades of identical signals peak at +3.01 dB; single 10 ms noise fades vary ±0.7 dB → level ACs pool 20 trials. Audition parity sources: Overwrite/Insert via right-click Record, Punch and Roll pre-roll 5 s.
- **Reference numbers (T-200):** dB = `20·log10(rms)`; 16-bit TPDF residual −96.33 dBFS (dither alone −98.09; undithered rounding −101.10); 24-bit TPDF −144.49. Per-bin white-noise offset: Hann@2048 −25.33 dB, Blackman-Harris@8192 −30.09 dB. Scalloping: 1 kHz at 48 k/FFT 2048 reads −20.63 dB (Hann) for a −20 dBFS sine — ACs use bin-centred tones. symphonia 0.6.1 has no gapless option → trim with `Track.delay/padding` (T-202 verifies which readers fill them). rubato 5 documents no stopband numbers → T-605 measures. Shift+D = Audition spectral toggle (verified). Open questions for T-202: symphonia PCM coverage (8-bit, 32i, 64f, A/µ-law), RF64 read, HE-AAC behaviour, flacenc MD5; manual marker interop check in REAPER/Audacity/ocenaudio.
- **From SPEC-008/010 (T-300):** T-301 — journal `edit` records need label params (e.g. "Normalize to −1 dB") → small ADR-004 amendment when T-301 is ticketed; undo/redo cursor rule from SPEC-008 §2.3. T-302 — materialize the clipboard before session GC, re-bind it to the new session after the first same-rate paste, cache converted pieces per document; `Piece.len` is u32 → split silence > ~24.8 h. T-305 — a pyramid-accelerated peak scan must be proven bit-exact vs brute force (SPEC-010 AC-15) and needs a "no non-finite samples" guarantee at chunk commit. ADR-003 event list gains `clipboard_changed`. SPEC-019 checklist: Crop = Ctrl+T, zero-crossing Shift+I/O/H/J/K/L, Mix Paste (sources disagree), Backspace as Delete, normalize keys (none by default). Adobe helpx/defkey/mikemurphy return 403; killerkeys.com is the best secondary shortcut source.
- Consider a `just deps-check` recipe enforcing ADR-001 rule 4 (forbidden crate edges).

## Owner requirements before the project can close (2026-09-15)
- Complete documentation for technical AND non-technical readers, with Mermaid diagrams → T-706 + T-707 (run last; refresh if later tickets change the architecture).
- Themes: Light ("clear") mode at full parity + Match System + High Contrast → T-708.
- Guided Tour widget teaching the app → T-709.
The project is NOT finished until T-706..T-709 are done.

## Ticket learnings
- **H-33** (Sonnet, 2026-09-15): `sanitize_preset_name` replaces a leading run of dots with `_` (still rejects exact `.`/`..`); `list_json_names` hides only `is_atomic_temp_leftover` (`.<name>.tmp-<digits>`). Timer races in component tests: use `vi.useFakeTimers()` + `vi.advanceTimersByTimeAsync(ms)`, never a real-timer `setTimeout` wait (WelcomeOffer de-flaked).
- **T-806** (Opus, 2026-09-15): VST3 hosting via the `vst3` crate 0.3.0 (coupler-rs; bindings from SDK 3.8.0, ADR-007 amendment; `LICENSE-VST3-SDK`), only the sandbox + `crates/test-vst3` link it. Backend `crates/sandbox/src/vst3/` (component + controller joined via connection points, host message objects, preallocated `IParameterChanges` queues, lock-free controller sync on the 10 ms idle tick, restart flags → RESTART_REQUEST, framed component+controller state). Values set while inactive: delivered via a zero-sample `process` during a brief pre-activation (else latency plugins restart forever). Mirror stores normalized values (continuous) / step indices (discrete) — sidecar stores normalized for VST3 (refines ADR-005 §14). Scan: `moduleinfo.json` bundles indexed without loading (relaxed JSON: the SDK writes trailing commas); others scanned sandboxed; format-neutral `PluginFormat` + `scan_plugin_files_with` + per-format tiers; cache still named `clap-scan.json` but holds both; bundles keyed by their binary's stamp. Install/uninstall `.vst3` into the per-user VST3 folder (a file picked inside a bundle = the bundle). `PluginFoldersDto.install_folders`. Gotchas: VST3 enum consts are `c_uint` on unix / `c_int` on Windows → module-level `allow(clippy::unnecessary_cast)`; tests waiting for a VST3 restart must keep processing blocks. No real VST3 plugins on this machine. Flaky: `sandbox/tests/teardown.rs::nothing_outlives_its_owner` fd baseline taken too early → H-34.
- **H-22** (Sonnet, 2026-09-15): presets — `crates/presets/src/export.rs` (`ExportedPreset` Module/Rack tagged `kind`; import returns name/module_id verbatim → always save through the stores, which sanitize), commands `module_preset_export/import`, `rack_preset_export/import`; `PresetError::AlreadyExists` → `error.preset_already_exists` with a `name` param → UI "Replace preset ‹name›?". `ManagePresetsDialog.svelte` (rack/module tabs; rename/delete/export user presets; factory read-only; import with overwrite confirm) opened from slot/rack "Manage presets…". Tauri dialog `open`/`save` mock in vitest via `mockIPC` on `plugin:dialog|open` / `plugin:dialog|save`. Found: `sanitize_preset_name` can yield a leading `.` ("../x" → ".._x") which `list_json_names` hides as an atomic temp file → H-33; `WelcomeOffer.test.ts` flaked once in the full suite → H-33.
- **H-31** (Sonnet, 2026-09-15): A-019 done — `resolveTheme(pref, prefersLight, prefersContrastMore=false)`; `system` → high-contrast when `(prefers-contrast: more)`, live listener + the same precedence in the `ui/index.html` boot script. Gallery has a High Contrast column. WebGL thick lines: `QuadBatch.line()` (`ui/src/lib/render/quads.ts`; no joins) — the raw-sample polyline is now quads drawn with `gl.TRIANGLES` at `themeColors().strokePx`. `previewIpc.ts` handles all transport commands and its default case console.errors + **throws under Vitest** for any unhandled command.
- **H-30** (Sonnet, 2026-09-15): export + save/save-as hold `SpectroService::begin_background_job()` (`ExportService::start` takes the spectro Arc; `DocumentService::set_spectro` OnceLock; `lib.rs` builds spectro first); `SpectroService::worker_cap_now()` proves a guard is held without racing tiles. All job services now emit the error notice BEFORE the terminal `Failed` (export/loudness/normalize/nr_capture fixed; loudness no longer toasts on user cancel; calibration gained `CalibrationEvent::Notice`). `DocumentService.bake_running` (separate from `normalize_busy`) → `rack_commands::apply` + `rack_preset_load` refuse with `error.bake.busy`. Test hooks: import sanitizes NaN to 0 (`ProjectError::NonFiniteSample` unreachable via a document); `plan_normalize_*` call `on_progress(0.0)` synchronously first — a deterministic mid-job hook.
- **H-32** (Sonnet, 2026-09-15): EQ graph blank at wide sizes = (1) `transport.svelte.ts::applyState` accepted a `null` reply (the preview IPC has no `transport_seek` case; `document.svelte.ts` auto-seeks on open) and corrupted `state` → now ignored; (2) EqGraph drew from `$effect(() => draw())`, which stops re-subscribing after a run that throws. **Every canvas renderer draws from a perpetual rAF loop started in `onMount` (try/finally reschedule), never a reactive `$effect(draw)`** — EqGraph converted, Waveform/Spectral/Analyzer loops hardened. Preview IPC still lacks transport command cases → H-31 item.
- **T-709** (Opus designer, 2026-09-15): guided tours in `ui/src/lib/tour/` (`tours.ts` content, `tour.svelte.ts` store, `progress.ts`, `tourPlacement.ts`, `targets.ts`, `TourOverlay.svelte` spotlight + step card, `WelcomeOffer.svelte`, `TourButton.svelte` "?"). Tours: Welcome (10), Rack (4), Noise (3), Loudness (4), Punch-in (3), Plugin Manager (4). Add a step = i18n keys + a `data-tour` anchor on a stable element + the step in `tours.ts`; bump the tour `version` to re-offer (never after "Don't show again"). `tourAnchors.test.ts` mounts the real app and fails on a missing anchor. `Settings.tours` (ToursSettingsDto/TourProgressDto/TourOutcome). Tour layer z 1050 (above dialogs, below toasts); the tour steps aside while a dialog opens unless its targets are in that dialog; tour keys are captured before app shortcuts. Dialog `headerActions` snippet; recovery store `checked` flag (the first-run offer waits for it). Preview `?preview&scene=tour&step=n[&tour=id]`, `&dialog=tour-offer`. Screenshot runs: Vite listens on `[::1]` → use `localhost`; ~5.5 s wait with 3 tabs.
- **H-29** (Sonnet, 2026-09-15): Uninstall (Plugin Manager ⋯ → Uninstall…, only for files inside the per-user install folder — canonicalized parent check; others offer Block): `install::uninstall_file` + `PluginCatalog::uninstall` (file, specs, scan-cache entry via `scan::cache_remove`, every live registry via new `Registry::remove`); open documents keep a Missing slot with the state blob. Duplicate ids (ADR-008 Amendment 5): tiers install folder → standard paths → custom folders (`scan::find_clap_files_ranked`), losers reported as `PluginStatusDto::Shadowed { by }`; shadow info refreshes only on load_cached/rescan. `PluginCatalog::install/uninstall` take `install_dir` explicitly (testable without real HOME). Plugin fixtures now in `ui/src/lib/test/fixtures.ts` (`pluginEntry`, `pluginFixtures`, `folderFixture`, `FLAGGED_ID`). Preferences "needs attention" = blocklisted + flagged (shadowed is informational).
- **T-708** (Opus designer, 2026-09-15): 4 themes: Dark (default), Light, Match System, High Contrast (AAA text, 3 px yellow focus ring, 2 px playhead/marker/record-head lines). One token block per theme in `design-tokens.css` (chrome + content tokens + line widths); `tokens.css`/`theme-bridge.css` hold no colours. Renderers read colours only via `theme/themeColors.ts::themeColors()` (typed, cached, invalidated on theme change → live repaint). `colorLiterals.test.ts` fails the build on any hex/rgb()/hsl()/named colour outside the token files + colormap tables. New theme = block in design-tokens.css + `RESOLVED_THEMES`/`THEMES` in theme.svelte.ts + Rust `ThemePref` variant (`themeParity.test.ts` enforces). No-flash: `localStorage["powervoice.theme"]` mirror + inline script in `ui/index.html` (Settings stays the source of truth). Spectrogram background stays dark in every theme (colormaps start at black). `?preview&theme=dark|light|system|high_contrast`; CDP screenshot runner = scratchpad `t708/shoot.mjs` (336 shots ≈ 4 min). WebGL sample lines stay 1 px in HC (Canvas2D 2 px).
- **T-602** (Opus, 2026-09-15): Effects → Bake Rack. `vox_engine::bake::render_document_range` (on `vox_rack::offline::render_range`) is THE offline render path for a document range — export and bake share it (bit-identical). Policy (SPEC-012 §2.8.1): length-preserving, no tail appended; pre-roll = real audio before the range, `min(start, clamp(sum of active tails, 30 s, 60 s))`; latency flush feeds the real audio after the range. Selection exports now get the same pre/post-roll (whole-file unchanged). History `history.bake` entry carries a `BakeAttachment {before, after}` RackModel (ADR-004 Amendment 7); undo/redo load it after the document lock is released. Write-back jobs share `normalize_busy` (`begin_bake_job`/`abandon_bake_job`/`finish_bake`); `SpectroService::begin_background_job()` guard halves tile workers. Commands `edit_bake_start`/`edit_bake_cancel`, `JobKind::Bake`. Job services emit the error notice BEFORE the terminal `Failed` progress event (orchestrator fix: the test raced under load). UI job stores buffer `job_progress` that beats the start command's reply.
- **T-809** (Opus designer, 2026-09-15): Plugin Manager (`ui/src/lib/plugins/`: PluginManagerDialog with Plugins/Folders tabs, InstallPluginDialog, `plugins.svelte.ts`, `pluginList.ts`), Effects → Manage Plugins… / Install Module…, Preferences → Plugins summary, amber flagged-slot button in the rack. Install = `vox_plugin_host::install` → per-user CLAP dir (`~/.clap`, macOS `~/Library/Audio/Plug-Ins/CLAP`, Windows `%LOCALAPPDATA%\Programs\Common\CLAP`; ADR-006 Amendment 1), scan only the new file, collision → confirm replace, failure → rollback + blocklist. `PluginCatalog` serializes rescans/installs (scan lock). New commands `plugins_clear_flag`, `plugins_folders`; blocklisted status carries a cause code. Kit: Dialog `size="xl"`, Toggle `hideLabel`. Headless screenshots: `chromium --headless=new --force-prefers-reduced-motion --virtual-time-budget=6000 --screenshot` (without reduced motion dialogs are captured mid-fade). Open: no uninstall; duplicate plugin id = last installed wins this session but first-in-path wins at the next full rescan; plugin fixtures still in `plugins/testing.ts` (move to `test/fixtures.ts`) → H-29.
- **H-18** (Sonnet, 2026-09-15): `ui/src/lib/test/fixtures.ts` = `settingsFixture`, `docDto`, `documentProbeDto`, `recordStateDto`, `transportStateDto`, `historyStateDto`, `paramInfoDto`, `rackSlotDto`, `rackStateDto` (complete defaults, typed returns). **New DTO/Settings field → update fixtures.ts only** (45 test files + `dev/previewIpc.ts` converted; test count unchanged). Rust side already used `..Default::default()` everywhere. Don't hand-roll local slot/param fixtures — use `rackSlotDto`/`paramInfoDto`.
- **H-28** (Sonnet, 2026-09-15): the recording-scene `effect_update_depth_exceeded` no longer reproduces after H-27 (`ViewportWriter` + reference-stable `INITIAL_*_FOLLOW_STATE`); a 200-tick jsdom burst test guards it. Transport race: `initTransport()` subscribes before `transportGet()` resolves, so an early frame seeded `PlayheadExtrapolator` against `doc_len_samples: 0` and `applyState` then skipped the real `playhead_samples` → a `ready` flag gates `onTelemetry` (not the `telemetryListeners` loop that `record.svelte.ts` needs). View → Follow Playhead (`menuitemcheckbox`, no shortcut in the spec). Dialog target fields use `units.ts::formatNumber/parseNumber` (Normalize dB/% and LUFS converted; `Number()` can't parse U+2212).
- **T-804** (Sonnet, 2026-09-15): `vox_plugin_host::catalog::PluginCatalog` = single source of truth for CLAP effects (cached specs, custom folders, crash-flag health store, `Weak<Registry>` observers); start-up `load_cached()` (no sandbox) then `rescan_in_background()` hot-adds via `Registry::upsert` into every composition root's registry. `vox_rack::Registry` is now `Mutex<BTreeMap>` — `get`/`ids`/`descriptors` return owned values; `plugins::registry()` returns `Arc<Registry>`. Files in the cache dir: `clap-scan.json` (schema 2: scan instantiates, never activates, for real ports/params), `plugin-blocklist.json` (path+size+mtime+CRC32; Crashed/TimedOut/Manual; auto-clears on change), `plugin-health.json` (runtime crash counters; `OfflineDeadline` faults not counted, RT-safe). `Settings.plugins {custom_folders, disabled}`; commands `plugins_list/rescan/set_enabled/block/unblock/add_folder/remove_folder` + `plugin_scan_progress`. Limits for T-806/T-809: scan fns are CLAP-named (add parallel VST3 fns, no trait yet); blocklisted rows have path only; no "missing" status. Flaky under full parallel load once: `document::tests::t301_recover_a_crashed_session_then_close_deletes_it`.
- **H-26** (Opus designer, 2026-09-15): every menu = `ui/Menu.svelte` fed a `MenuEntry[]` (menu-bar menus via `menu/MenuBarMenu.svelte`; popovers via `ui/Popover.svelte`; WAI-ARIA menu keyboard) — no hand-rolled dropdowns; old row components + `menuKeyboard.ts` deleted. Dialog footers = `actions={[{label, role, onclick, testid}]}` ordered per platform by `ui/dialogActions.ts` (tests: `setPlatformForTest`). All numeric labels via `units.ts::formatNumber` (U+2212; parser accepts `-`/U+2212/dashes). Axis labels via `ui/axisLabels.ts`, units get their own slot; EQ layout `eq/axisLayout.ts`. `@lucide/svelte` replaced `lucide-svelte` (main checkout needs `npm install --prefix ui` after merge, then touch `ui/vite.config.ts`). `?preview&scene=document|spectral|recording|rack|loudness&dialog=…&menu=…`. Gotchas: a CSS `display` rule beats the `hidden` attribute (Loudness tab bug) → pair with `[hidden]{display:none}`; headless `?preview` screenshots need a CDP script (chromium `--virtual-time-budget` hangs on the spectral scene); restart Vite after many HMR updates (stores load twice). Open: `?preview&scene=recording` threw `effect_update_depth_exceeded` from WaveformView's follow effects (pre-H-27) and transport store null `state` when telemetry beats `transport_get` → H-28.
- **H-27** (Sonnet, 2026-09-15): playback band-follow (SPEC-006 §2.8, 10–90 % band, shift by overflow, resume when the playhead re-enters the band or at the next play) = pure `ui/src/lib/waveform/playbackFollow.ts`; `viewportFollow.ts::ViewportWriter` is THE shared "my write vs the user's" diff — one unified `$effect` in WaveformView runs page-flip (record op) or band-follow (playing + `Settings.playhead_follow`, default true). Spectral view + ruler follow via the shared viewport state. `Settings.playhead_follow` had no menu toggle at merge (follow-up).
- **H-23** (Sonnet, 2026-09-15): no PostRoll phase once `op.output_lost` (stays Recording until RunOver → Committing); record-head follow = pure `ui/src/lib/waveform/recordFollow.ts` (page-flip, user scroll/zoom suspends until the next page) hooked into WaveformView via a `$effect` with a last-set-value diff; AC-13 length-unknown: `FakeDirection::unreliable_timestamps()` → `StreamInfo::timestamps_reliable` → `InputCmd::TimestampsReliable`; callback-gap fallback (1.5× period) reports `UNKNOWN_GAP` → `DropoutMark::UNKNOWN_LEN` (`u64::MAX`) → point marker "Dropout (length unknown)". Gotchas: **playback playhead follow (SPEC-003 §2.1/§3, SPEC-006 §2.8, continuous band-follow) is spec'd but NOT implemented** → H-27; `assert_no_alloc` warn-mode prints "Tried to (de)allocate…" for `rig_with`'s deliberate self-test probe under `--nocapture` (benign).
- **T-803** (Opus, 2026-09-15): CLAP hosting — `crates/clap-abi` (hand-written CLAP 1.2 subset, layout-tested, MIT headers credited in notices; no clack), `crates/test-clap` (cdylib test plugin: mono gain w/ 64-sample latency, stereo-only gain for the mono shim, latency param → `request_restart`; `crash-on-scan`/`hang-on-scan` copies), CLAP backend in `crates/sandbox/src/clap/` (control reader thread + 10 ms main-thread tick for plugin callbacks), `--scan <file> --format clap`, sandbox-ipc protocol v2, `plugin-host::scan` (CLAP_PATH first, one sandbox per file, 30 s timeout, path+size+mtime cache; failures not cached → retry each start until T-804's blocklist), `ModuleFactory::loads_async` → rack creates/activates on a worker (`SlotStatus::Loading`, `RackNotice::SlotLoaded`), host-internal `ParamText` extension (plugin's own param text). Gotchas: tests must tick until `RackHost::is_loading()` is false and compare bit-exact only after latency + 15 ms fade; a cdylib dev-dependency is built at `target/<profile>/deps/lib<name>.so` (locate via `CARGO_BIN_EXE_powervoice-sandbox`); `PR_SET_DUMPABLE 0` does not stop systemd-coredump here — intentional test crashes set `RLIMIT_CORE=0` first; `RackSlot.texts` + `SlotStatusDto::loading` are new DTO fields; `POWERVOICE_NO_PLUGIN_SCAN=1` skips the start-up scan.
_(appended after each merged ticket)_
- **S3-02** (Opus, merged, no separate review — measured numbers far inside tolerance): `org.powervoice.dynamics@1.0.0` (compressor + limiter live; AutoGate/Expander/look-ahead params exist with permanent ids but carry `HIDDEN` and do nothing until T-407 / look-ahead work), `org.powervoice.noise-gate@1.0.0`. Gotchas: gate snaps to its floor within 1e-6; sidechain HPF onset transients can open a −40 dBFS gate from an abrupt LF start → rumble tests fade in; one-pole timing counts the step sample; fetch telemetry handles per instance; clippy flags `next` on non-iterators. Module i18n keys (`module.dynamics.*`, `module.noise_gate.*`, `module.true_peak_limiter.*`, `param.gain.*`) are not in `en.json` yet → S3-01.
- **H-04** (Opus, merged): output-callback invariants — a start while audible always fades out first; a rack reset waits until silence covers the rack's latency; a pending reset survives a later non-reset Play; END during a fade finishes it. `close_output` pauses a playing transport on every close path (S1-04 can rely on it). The control loop ticks after every message. New `DeviceNotice::ResampleUnavailable` (`notice.device.resample_unavailable`). Test patterns: `rig_with(.., setup)`; hit callback boundaries by counting `recorded_output(id).blocks`; judge transport clicks with the §4.3 tone + `analyze_zipper` at T_s = 5 ms; reproduce inbox starvation with a pre-filled channel, not synchronous `EngineHandle` floods.
- **S3-04** (Opus, merged, no separate review — measured numbers well inside tolerance): `org.powervoice.noise-reduction@1.0.0` (vox-dsp `nr/`: profile capture + 32 824-byte blob v1 + `derive_lambda`, streaming `SpectralNr`, latency exactly N, tail 2N, params bind per frame, no print = exact N-sample delay). ~12 dB noise drop with 0.03 dB voice change; realtime == offline bit-identical; CPU 0.23 % of a core. SPEC-014 Amendment 1 records the accepted deviations. Handoff to **S3-06**: `NoiseProfile::describe` distinguishes unreadable (`Resource`) vs too new (`Unsupported("newer")`); empty blob = "none" (host shows it); `capture` enforces 0.5 s min / 60 s cap / finite input — short/loud/60 s warnings are host-side; `NoiseReduction::state_with_blob` builds a default state with a print. Gotchas: frequency-domain modules report tail ≥ 2N; `ZipperTest`'s 3.0 s signal can't fit long drags + latency; Ms/Hz params > 1000 display with 10 ms/10 Hz resolution ("1.69 s"); dB-domain frequency smoothing pulls a pure tone ~−0.3 dB at 20 dB SNR (watch AC-7).
- **S3-05** (Opus, merged): `org.powervoice.true-peak-limiter@1.0.0`, latency L + 16; input gain applies at k + L + D (SPEC-017 Amendment 1); testkit has `true_peak` (16× reference) and `bandlimit` modules for DSP tests. Tests that count `builtin_factories()` break when modules are added — look modules up by id. Latency modules whose detector reads ahead must delay parameter changes by the full latency.
- **S1-01** (Opus, 2026-09-13, merged; post-merge Opus review pending): **Engine API** — `Engine::start(EngineConfig{backend, registry, rack, prefs, events, clock}) → Engine`; `.handle()` → `EngineHandle` (Clone, sync): `transport(TransportCommand::{Play, Pause, PlayPause, Stop, PlayFromStart, ReturnToStart, Seek(u64)}) → TransportState`, `transport_state()`, **`set_document(Option<PlaybackDoc{store: Arc<ChunkStore>, snapshot: Arc<DocSnapshot>}>)`** (stops playback first, reopens output if the rate changes — call after open and after every edit), `set_selection(Option<(u64,u64)>)` (stub), `devices()`, `select_devices(DevicePrefs)`, `set_telemetry_sink(..)`. **Never call `EngineHandle` from an `EventSink`/telemetry sink (control thread → deadlock).** S1-04 plugs input + recording into `Control` (`Activity.recording`, `DeviceAction::StopRecording`, input streams currently ignored). Tests: `ManualEngine` (no threads, inline reader) + fake time: `poll_devices()`, then `fake.advance_by(1 ms)` + `tick()`. UI: `ui/src/lib/transport/playhead.ts` (extrapolation/slew/clock sync, reuse in the waveform), `ipc/telemetry.ts` (VXTM). Decisions: document end = Pause at the end; seek doesn't change the play-start position, Play from start does; output RMS = energy average per frame; "System default" output = `output_device: null`. Gotchas: PipeWire adds our own stream node (15 → 16 devices ~1 s after opening); `pkill -f <worktree path>` kills the calling bash — use `pgrep -af "[w]orktrees/…"`; a `TS_RS_EXPORT_DIR` fixture written by an `export_bindings_*` test is staleness-checked by `check-types`. Deferred: loop, follow, full banner table, Rescan, input-channel dropdown, xrun UI, meter ballistics, destructive-edit stop→ack→commit (S2-01 must add it).
- **T-101 fix round** (1 round, 2 blocking): **durability rule — a chunk is durable only after `ChunkStore::sync()`; `Session` is the only code that syncs before a journal record referencing new chunks; any future writer of `chunks` records (T-202 import, T-301 compaction) must go through `Session`.** Import 60 min: 0.88 s + 0.11 s sync (was 8.1 s). `Session::set_floor` for imports (T-202 must use it). `commit_take(&FinishedTake, &[Marker])` borrows + clamps markers; markers pressed during a take go to `commit_take`, not `commit_edit`. `close()` → `Err(CloseError { error, session })` on refusal. **T-106 runs `TakeSyncHandle::sync()` ~every 1 s on its own thread** (`TakeSyncMode::Background` default). `unreserved_segments()` → engine warns on CoW filesystems. ADR-004 Amendment 5 records all of this.
- **T-102 fix round** (1 round, 3 blocking): timestamps carry `now_ns` (app clock) — **T-105/T-106 must use it and never read the clock again**; `to_app_ns()` maps stream instants; `app_now_ns()`/`app_epoch()` process-wide epoch. Fake input delivered at `time(F+N)+latency` (+ optional seeded jitter). `DeviceInfo` has `base_name`, `id`, `system_default`, `input_is_monitor`, per-direction `Option<CapsState>` (Known/Pending/Unavailable); two-phase enumeration (`caps_pending`); one call per device callback (conversion scratch sized from the opened buffer); `StallDetector` (max(500 ms, 4 periods)) — **T-105's control tick must call it and treat a stall as device loss**; callbacks dropped on cpal's thread → hand heavy state back via a slot. alsa-lib prints "Found no matching channel map" to stderr during caps reads (harmless); cpal JACK devices always offer 1…64 ports (now clamped to defaults).
- **T-102 re-review nits → handoff:** T-109 keeps a "show all inputs" option (monitor heuristic misses PipeWire filter-chain sinks named `*input*`); **T-105 control thread remembers the configured device `id` and `lookup` prefers it** (else an unplugged twin silently reopens the other twin); ALSA PCMs routed to PipeWire (`pipewire`, `default`) still report 64 channels (ALSA-host only); cached "unavailable" caps are re-read only on Rescan. T-105: run `StallDetector` every tick → `DEVICE_LOST`; wait for the caps diff while `caps_pending` (ALSA ~2.4 s).
- **T-103 fix round** (1 round, 1 blocking = new instances with latency faded in before their output was valid): crossfades now have a **hold phase = the new instance's reported latency** before the 15 ms fade (a new instance is heard after latency + 15 ms; SPEC-012 §2.2 amended); `RackCommand::Remove { slot, hold }` for reorders; a second replacement of the same slot is held back until `ReplaceDone`; A/B dry-tap swap deferred during an A/B fade; 64 lifecycle entries reserved in outbox + RT ring; outbox drops counted in `dropped_rt_events()`; teardown via **`RackHost::teardown(live)` / `LiveRack::into_chains()`**; `RackHost::replace_state(index, ModuleState)` (presets, noise print); `ParamChanged` coalesced per (slot, id) per tick; load-time start failures → `SlotStatus::Failed` "Couldn't start …"; `take_over` moves queued events. Probe margins: insert latency module −31.5 dB, restart −64.5 dB, restart 480→960 −29.8 dB (no silence), reorder −38.5 dB.
- **T-103** (Opus, 2026-09-13, in review): `vox-rack` rewritten — `Chain` (per-slot latency-matched dry + 15 ms linear crossfade for bypass/insert/remove/replace, non-finite guard, coalescing event queue, RT outbox), `LiveRack` (audio side: command ring ≤ 256/call, pointer-swap chains keeping untouched instances, A/B, return ring), `RackHost` (control side: model + param mirror, off-thread build/activate, restart-driven replacement, ≤ 32 swaps in flight, notices `ParamChanged`/`LatencyChanged`/`SlotFailed`/`SlotRestarted`), `Registry`, `RackModel`/`SlotModel` (placeholders keep verbatim JSON), `DualMonoShim`, `offline::render(_with_automation)` (refuses placeholders → `MissingModules`). `Gain` = `org.powervoice.gain@1.0.0`; `builtin_factories()`. **T-105 integration:** `RackHost::new(registry, cfg, options, model) → (RackHost, LiveRack)`; output callback calls `live.process(transport, in, out)` (any length, sub-blocks internally); `live.latency_samples()` for the heard position; control thread calls `host.tick()` every 16 ms; `LiveRack::handle(cmd)` exposed. §4.3 harness in `vox_module_api::test_util` (own radix-2 FFT); for crossfade tests call `run_until(S)` then send the command; a 0.01 single-sample click (~−104 dBFS/bin) passes — clicks must be ~0.1 to fail. Rack `Driver` asserts `alloc_checks_active()`. Offline render sets no FTZ/DAZ yet (T-105 adds the dsp guard). Open: reorder restarts moved modules (smooth dip ≤ latency + 15 ms); `ParamChanged` echoes every set → T-108 should coalesce per tick. API break: T-005 `Rack` → `Chain`.
- **T-102** (Opus, 2026-09-13, in review): `vox-engine` backend trait + cpal (PipeWire/JACK/ALSA all build here: libpipewire 1.6.8, pipewire-jack, libclang 22) + deterministic `FakeBackend`, `devices.rs` pure fallback/resolve/deinterleave/`MonoInput` (atomic channel switch), `DevicePollThread`, `device_state.rs` machine (output-only loss while recording keeps recording; `StopPlayback` must not stop recording — **T-105 contract**), `prefs.rs` `DevicePrefs` (intent never mutated). Real machine: hosts pipewire (default, 15 devices, 6 ms), jack (2), alsa (63 devices, **2.8 s first enumeration**, 56 ms cached). PipeWire: only 48000 Hz offered (44.1 k docs resample), `playback − callback` 53.3 ms at 1024 quantum (timestamps meaningful), lists `default_*` pseudo-devices and sink monitors as inputs. Gotchas: cpal PipeWire host snapshots devices at `Host::new()` → rebuild per poll; cpal JACK `is_available()` always true; cpal 0.18 `description()?.name()`, `DeviceId` = stable `host:id`, `build_*_stream_raw` 5 args; cpal drops callback closures on its own thread when a stream ends → callbacks must not own objects that need the return ring. Fake: callback − capture = configured input latency exactly; callbacks must not call into `FakeBackend`; RT guard via `set_rt_guard(|f| no_alloc(f))`. `DeviceNotice` carries data only (i18n mapping in T-108/T-109). `vox-engine` depends on `module-api` only as a dev-dependency.
- **T-101** (Opus, 2026-09-13, in review): `vox-project` session/store/snapshot/reader/take/journal/history/gc. Semantics: `rev` and `audio_rev` are monotonic **but undo/redo produce fresh revs → compare with `!=`, never `<`**; dirty = current seq ≠ saved seq; markers inside a replaced range clamp to the end of the inserted material (range markers map start/end independently); marker-only edits allowed while recording; journal line = `<crc32 lowercase hex>\t<json>`, records incl. `take_discard`, `edit.take`, hex attachments; only the session-start checkpoint (periodic checkpoints → T-301). Engine usage: call `SnapshotReader::release_segments()` when idle; after `TakeCapture::finish()` check `FinishedTake.error` then `commit_take`; drop all readers/writers before `Session::close`. **60-min import = 8.1 s (target < 3 s) — likely synchronous per-chunk flush → T-202 must measure; fix = async flush + one sync before the journal record.** Gotchas: std `File::try_lock` = flock (per open file description); memmap2 `map_raw` safe, `flush_range` page-aligns; clippy 1.98 flags `chunks_exact(N)` const → `as_chunks::<N>()`; `/tmp` is **tmpfs** on the owner's machine → big tests use `target/big-tests` via `POWERVOICE_TEST_TMP`; `just test-big` recipe. New i18n keys needed: `error.not_while_recording`, `error.disk_full`, `error.io`, `error.store_corrupt`, `error.cancelled`, `error.session_locked`, `error.invalid_edit`, `error.internal`. macOS F_PREALLOCATE path uncompiled (no Apple target).
- **T-104** (Sonnet, 2026-09-13): `src-tauri` logging (`~/.local/state/powervoice/logs`, `POWERVOICE_LOG`), panic hook → `crashes/`, versioned settings (`~/.config/powervoice`, atomic, unknown fields preserved, v0→v1 hook, `settings_get/set`), `notice` event with toast/persistent banner (`id` replaces), expanded `IpcErrorCode`, keymap registry (`ui/src/lib/keymap`), runes stores, WebKit DMA-BUF default in `main()` + justfile (opt-out `POWERVOICE_WEBKIT_DMABUF=1`). `DevicePrefsDto` is a src-tauri DTO → **T-108 maps it to T-102's engine `DevicePrefs`**. Settings DTOs derive ts-rs in `settings.rs` (src-tauri only, allowed). Gotchas: `directories::ProjectDirs::from("app","powervoice","powervoice")`; macOS/Windows have no state_dir → logs under data_dir; `ipc_events!` names must be re-exported from `ipc/mod.rs` or dead-code fails `-D warnings`; jsdom lacks `isContentEditable` → check the `contenteditable` attribute; `tDynamic()` for runtime i18n keys. src-tauri declares deps as plain versions (not workspace) — follow that locally.
- **T-009** (Sonnet, 2026-09-12): code renamed to PowerVoice — packages/binaries `powervoice-cli`, `powervoice-app` (lib `powervoice_app_lib`), identifier `app.powervoice.editor`, ids `org.powervoice.*`, env `POWERVOICE_*`; internal `vox-*` crates kept; `license = "MIT OR Apache-2.0"` everywhere. Gotcha: renames that lengthen string literals can break `cargo fmt --check` — run `cargo fmt --all` after identifier renames. ts-rs bindings contain no product name.
- **T-007** (Sonnet + orchestrator Opus review, 2026-09-12): ADR-009 — WebGL2 primary, Canvas2D fallback (feature-detect `webgl2`, fall back on `webglcontextlost`); IPC `Response`/`Channel` raw payloads arrive as `ArrayBuffer`, ~80–88 MB/s for 10 MB; **telemetry default 60 Hz** (free); `WEBKIT_DISABLE_DMABUF_RENDERER=1` roughly halves frame time on AMD/Mesa too (p50 17 → 10 ms, ~59 → ~100 fps; slightly higher worst-case spikes); Hyprland doesn't throttle rAF for visible-but-unfocused windows. Gotchas: Tauri commands taking `AppHandle<R>` must stay generic over `R: tauri::Runtime` to work inside `ipc_commands!`; automation must race rAF/IPC steps against `setTimeout` watchdogs (one unexplained >15 min hang seen once). `just spike` (+ `POWERVOICE_SPIKE_EXIT=1`) is the reusable harness.
- **T-005 review outcome** (1 fix round; 1 blocking = derived Clone dropping EventList capacity): `ModuleTestHost` immediate-effect check is now **opt-out** — built-in modules must declare deliberately delayed params (time constants, thresholds, neutral-section enables) via `ModuleTestHost::allow_delayed_effect(id)`; event-timing/flush/state checks are latency-aware and run in `Offline` mode; `display_decimals()` derives text precision from step (exact round-trip); `ProcessStatus` is `#[non_exhaustive]` (deviation from ADR-005 sketch); workspace `serde_json` has `float_roundtrip`; `clippy.toml` disallows raw `assert_no_alloc`.
- **T-005** (Opus, 2026-09-12): ADR-005 implemented in `vox-module-api` (+ `test-util`: `ModuleTestHost`, `TestGain`, `TestRng`, `install_test_allocator!()`) and `vox-rack` skeleton. Gotchas: `serde_json` needs feature `float_roundtrip` for bit-exact f64 (sidecar in `project` must enable it); `assert_no_alloc` workspace dep has no default features, module-api enables `warn_debug`+`warn_release` (never enable `disable_release` → compile_error); every test binary using `ModuleTestHost` must call `vox_module_api::install_test_allocator!();`; clippy wants `.is_multiple_of(n)` instead of `x % n == 0`. Orchestrator decisions: stepped params' display decimals derive from step (exact text round-trip); module registry → T-103; T-103 coalesces same-offset same-id param events in place (latest wins).
- **T-006** (Sonnet + Opus review, 2026-09-12; 1 fix round, 2 blocking findings): `vox-testkit` = generators (PCG32 verified vs reference vector, Kellet pink, sweeps, bursts, Tech 3341/3342 cases), measurements, golden helpers, WAV I/O; `powervoice-cli gen|analyze [--json]`, `gen-fixtures` bin (`just fixtures`, 60-min file streams at ~15 MB RSS). **Measurement conventions:** `-inf` = digital silence, `NaN` = non-finite input anywhere (never masquerades as silence), `None`/"n/a" = input shorter than the window (noise floor 500 ms, momentary 0.4 s, short-term 3 s); JSON uses `null` for −inf/NaN. Noise generator levels are **RMS dBFS**. Noise floor is per channel, worst reported. `hound::WavWriter` accepts `Cursor<&mut Vec<u8>>`. `ebur128` only exposes current M/S windows → poll every 100 ms for max values.
- **T-004** (Sonnet, 2026-09-12): Tauri 2.11.5 / @tauri-apps/api 2.11.1 / cli 2.11.4, Svelte 5.57, Vite 8.3, Vitest 5.0, TypeScript 6.0, jsdom 30. Gotchas: `cargo test -p X` runs with cwd = package dir → pass absolute paths (`{{justfile_directory()}}`) for `TS_RS_EXPORT_DIR`; `#[tauri::command]` emits a hidden macro → import command modules by glob for `generate_handler!`; Vitest needs `resolve.conditions: ["browser"]` for Svelte `mount()`; Tauri CLI run from `ui/` needs `TAURI_APP_PATH=../src-tauri`; plain `cargo test` writes ts-rs output to `src-tauri/bindings/` (gitignored). `check-types` diffs only ts-rs-generated files. Main tree needs `npm --prefix ui ci` after merges touching `ui/package-lock.json`.
- **T-001** (Haiku, 2026-09-12): workspace + 9 crate skeletons, lints, justfile, `.githooks/pre-commit` (fmt check; `core.hooksPath` set). `just check` UI steps auto-activate once `ui/package.json` exists. Parallel tickets that edit root `Cargo.toml`/`justfile` must run in worktrees from now on.
