[package]
name = "vox-rack"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
vox-module-api = { path = "../module-api" }
vox-dsp = { path = "../dsp" }
thiserror.workspace = true
serde.workspace = true
serde_json.workspace = true
rtrb.workspace = true

[dev-dependencies]
vox-module-api = { path = "../module-api", features = ["test-util"] }
vox-modules = { path = "../modules" }
vox-testkit = { path = "../testkit" }
# T-110: the full voice rack's process() cost bench (`benches/full_rack.rs`).
divan.workspace = true

# T-110: the assembled 6-module voice rack's process() cost, 64..1024-frame blocks, against the
# PROMPT §2 "< 20% of one core" target.
[[bench]]
name = "full_rack"
harness = false

[lints]
workspace = true
